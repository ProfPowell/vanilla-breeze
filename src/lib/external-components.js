/**
 * External web components loader
 *
 * Imports code-block and browser-window custom elements.
 * Use this file via <script type="module" src="/src/lib/external-components.js">
 * instead of inline imports, so Vite can resolve the bare specifiers.
 */
import '@profpowell/code-block';
import '@profpowell/browser-window';
