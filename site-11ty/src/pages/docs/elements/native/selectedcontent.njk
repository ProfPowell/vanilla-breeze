---
layout: layouts/element.njk
title: "selectedcontent"
description: "Displays a clone of the currently selected option's content inside a customizable select's button trigger. Used with appearance: base-select to show rich content (icons, images, descriptions) in the closed select state."
elementName: selectedcontent
category: native
nativeGroup: Forms
currentPath: /docs/elements/native/selectedcontent/
permalink: /docs/elements/native/selectedcontent/
---

{% set basicCode %}<select name="status">
  <button>
    <selectedcontent></selectedcontent>
  </button>
  <option value="open">Open</option>
  <option value="closed">Closed</option>
  <option value="pending">Pending</option>
</select>{% endset %}

{% set richCode %}<select name="language">
  <button>
    <selectedcontent></selectedcontent>
  </button>
  <option value="en">
    <span aria-hidden="true">&#x1F1FA;&#x1F1F8;</span>
    English
    <span class="detail"> &mdash; American English</span>
  </option>
  <option value="es">
    <span aria-hidden="true">&#x1F1EA;&#x1F1F8;</span>
    Espa&ntilde;ol
    <span class="detail"> &mdash; Spanish</span>
  </option>
  <option value="fr">
    <span aria-hidden="true">&#x1F1EB;&#x1F1F7;</span>
    Fran&ccedil;ais
    <span class="detail"> &mdash; French</span>
  </option>
</select>{% endset %}

{% set hideDetailCode %}/* Hide supplementary text in the closed trigger */
selectedcontent .detail {
  display: none;
}

/* Hide images in the button, show only in dropdown */
selectedcontent img {
  display: none;
}{% endset %}

{% set structureCode %}<!-- Required structure -->
<select>
  <button>                        <!-- Custom trigger -->
    <selectedcontent></selectedcontent>  <!-- Cloned content appears here -->
  </button>
  <option value="a">Option A</option>  <!-- Options after the button -->
  <option value="b">Option B</option>
</select>

<!-- Both opening and closing tags are mandatory -->
<!-- This will NOT work: <selectedcontent /> -->{% endset %}

<section>
  <h2>When to Use</h2>
  <ul>
    <li>Inside a customizable <a href="/docs/elements/native/select/">select</a> to show a custom trigger with rich content</li>
    <li>When options contain images, icons, or descriptions that should be mirrored in the closed state</li>
    <li>When you need CSS control over how the selected value displays in the button</li>
  </ul>
  <p><strong>Requirements:</strong> Only works when the browser supports <code>appearance: base-select</code> (Chrome 135+). In non-supporting browsers, the select falls back to its standard rendering.</p>
</section>

<section>
  <h2>How It Works</h2>
  <p>The browser automatically clones the currently selected <code>&lt;option&gt;</code>'s DOM into the <code>&lt;selectedcontent&gt;</code> element using <code>cloneNode()</code>. It updates every time the selection changes via a <code>change</code> event.</p>

  <browser-window src="/docs/examples/demos/customizable-select.html" url="https://vanilla-breeze.dev/select/selectedcontent" title="Customizable select with selectedcontent" shadow></browser-window>
</section>

<section>
  <h2>Required Structure</h2>
  <p>The nesting must follow this exact pattern:</p>

  <code-block language="html" label="Required nesting structure">{{ structureCode | escape }}</code-block>

  <ul>
    <li><code>&lt;button&gt;</code> must be the <strong>first child</strong> of <code>&lt;select&gt;</code></li>
    <li><code>&lt;selectedcontent&gt;</code> must be inside that <code>&lt;button&gt;</code></li>
    <li>All <code>&lt;option&gt;</code> elements come after the button</li>
    <li>Both opening and closing tags are mandatory</li>
  </ul>
</section>

<section>
  <h2>Examples</h2>

  <h3>Basic</h3>
  <p>A plain select with a custom trigger. The selected option text is cloned into the button automatically.</p>
  <code-block language="html" label="Basic selectedcontent">{{ basicCode | escape }}</code-block>

  <h3>Rich Content with Flags</h3>
  <p>Options with emoji flags and descriptions. The <code>.detail</code> text is hidden in the closed trigger via CSS to keep it compact.</p>
  <code-block language="html" label="Rich content with selectedcontent">{{ richCode | escape }}</code-block>
</section>

<section>
  <h2>Styling</h2>
  <p>Since <code>&lt;selectedcontent&gt;</code> contains a clone of the selected option's DOM, you can target child elements to control what is visible in the closed state versus the open dropdown.</p>

  <code-block language="css" label="Hiding content in the trigger">{{ hideDetailCode | escape }}</code-block>

  <p>VB applies these styles automatically inside <code>@supports (appearance: base-select)</code>:</p>
  <ul>
    <li><code>selectedcontent</code> gets <code>display: flex</code> with <code>align-items: center</code> and <code>gap</code></li>
    <li><code>selectedcontent .detail</code> is hidden by default</li>
    <li>The parent <code>button</code> fills the select width as a flex container</li>
  </ul>
</section>

<section>
  <h2>Attributes</h2>
  <p><code>&lt;selectedcontent&gt;</code> accepts only <a href="https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes">global HTML attributes</a> (class, id, style, data-*, etc.). There are no element-specific attributes.</p>
</section>

<section>
  <h2>Accessibility</h2>
  <ul>
    <li><strong>Inert by default</strong>: The <code>&lt;button&gt;</code> inside <code>&lt;select&gt;</code> is inert, so <code>&lt;selectedcontent&gt;</code> and its children cannot be focused or interacted with independently</li>
    <li><strong>Implicit ARIA role</strong>: <code>none</code> &mdash; screen readers treat it as presentation</li>
    <li><strong>Cloned content</strong>: Decorative elements (icons, flags) in options should use <code>aria-hidden="true"</code> since they are cloned into the trigger as well</li>
    <li><strong>Keyboard navigation</strong>: Fully handled by the parent <code>&lt;select&gt;</code> &mdash; no additional keyboard handling needed</li>
  </ul>
</section>

<section>
  <h2>Caveats</h2>
  <ul>
    <li><strong>Dynamic content</strong>: The browser only updates <code>&lt;selectedcontent&gt;</code> on <code>change</code> events. If you modify the currently selected option's content dynamically (e.g., via a framework), the clone won't refresh automatically</li>
    <li><strong>Implicit button</strong>: If you omit <code>&lt;button&gt;</code> and <code>&lt;selectedcontent&gt;</code>, the browser creates an implicit button that cannot be targeted with CSS type selectors</li>
    <li><strong>Experimental</strong>: This element is not yet Baseline &mdash; check <a href="https://caniuse.com/?search=selectedcontent">browser support</a> before relying on it in production</li>
  </ul>
</section>

<section>
  <h2>Browser Support</h2>
  <p>Requires <code>appearance: base-select</code> support (Chrome 135+). VB gates all related styles behind <code>@supports</code>, so non-supporting browsers fall back gracefully to the standard select rendering.</p>
</section>

<section>
  <h2>Related</h2>
  <ul>
    <li><a href="/docs/elements/native/select/">select</a> &mdash; Parent element; see the Customizable Select section for the full <code>base-select</code> guide</li>
    <li><a href="/docs/elements/native/option/">option</a> &mdash; Options whose content gets cloned into selectedcontent</li>
    <li><a href="/docs/elements/native/optgroup/">optgroup</a> &mdash; Group related options with styled section headers</li>
    <li><a href="/docs/elements/native/button/">button</a> &mdash; The required parent of selectedcontent inside a customizable select</li>
    <li><a href="/docs/elements/web-components/combo-box/">combo-box</a> &mdash; Alternative for filterable dropdowns with full browser support</li>
  </ul>
</section>
