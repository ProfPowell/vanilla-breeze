---
layout: layouts/base.njk
title: "Contact"
description: "Get in touch to discuss your next project"
---

<section class="contact-page">
  <layout-center data-layout-max="narrow">
    <layout-stack data-layout-gap="xl">
      <header class="contact-header">
        <h1>Get in Touch</h1>
        <p>Have a project in mind? I'd love to hear about it. Fill out the form below or reach out directly.</p>
      </header>

      <div class="contact-grid">
        <form class="stacked" action="#" method="POST">
          <layout-stack data-layout-gap="l">
            <div class="form-group">
              <label for="name">Name</label>
              <input type="text" id="name" name="name" required />
            </div>

            <div class="form-group">
              <label for="email">Email</label>
              <input type="email" id="email" name="email" required />
            </div>

            <div class="form-group">
              <label for="subject">Subject</label>
              <input type="text" id="subject" name="subject" required />
            </div>

            <div class="form-group">
              <label for="message">Message</label>
              <textarea id="message" name="message" rows="5" required></textarea>
            </div>

            <button type="submit" class="button primary">Send Message</button>
          </layout-stack>
        </form>

        <aside class="contact-info">
          <layout-stack data-layout-gap="l">
            <div class="info-item">
              <icon-wc name="mail" size="md"></icon-wc>
              <div>
                <h3>Email</h3>
                <a href="mailto:hello@example.com">hello@example.com</a>
              </div>
            </div>

            <div class="info-item">
              <icon-wc name="map-pin" size="md"></icon-wc>
              <div>
                <h3>Location</h3>
                <p>San Francisco, CA</p>
              </div>
            </div>

            <div class="social-section">
              <h3>Follow Me</h3>
              <div class="social-links">
                {% for social in site.social %}
                <a href="{{ social.url }}" aria-label="{{ social.name }}">
                  <icon-wc name="{{ social.icon }}" size="sm"></icon-wc>
                </a>
                {% endfor %}
              </div>
            </div>
          </layout-stack>
        </aside>
      </div>
    </layout-stack>
  </layout-center>
</section>

<style>
  .contact-page {
    padding: var(--size-4xl) var(--size-l);
  }

  .contact-header {
    text-align: center;
  }

  .contact-header h1 {
    font-size: var(--font-size-4xl);
  }

  .contact-header p {
    font-size: var(--font-size-lg);
    color: var(--color-text-muted);
    max-width: 50ch;
    margin-inline: auto;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: var(--size-3xl);
    align-items: start;
  }

  @media (max-width: 768px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }

  .contact-form {
    background: var(--color-surface);
    border: var(--border-width-thin) solid var(--color-border);
    border-radius: var(--radius-l);
    padding: var(--size-xl);
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: var(--size-xs);
  }

  .form-group label {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
  }

  .form-group input,
  .form-group textarea {
    padding: var(--size-s) var(--size-m);
    border: var(--border-width-thin) solid var(--color-border);
    border-radius: var(--radius-m);
    font-size: var(--font-size-base);
    font-family: inherit;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: 2px solid var(--color-focus-ring);
    outline-offset: 2px;
  }

  .info-item {
    display: flex;
    gap: var(--size-m);
  }

  .info-item icon-wc {
    color: var(--color-primary);
  }

  .info-item h3 {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin-block-end: var(--size-2xs);
  }

  .info-item a {
    color: var(--color-text);
  }

  .social-section h3 {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin-block-end: var(--size-s);
  }

  .social-links {
    display: flex;
    gap: var(--size-s);
  }

  .social-links a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: var(--radius-full);
    background: var(--color-surface);
    border: var(--border-width-thin) solid var(--color-border);
    color: var(--color-text-muted);
    transition: all var(--duration-fast) var(--ease-out);
  }

  .social-links a:hover {
    background: var(--color-primary);
    border-color: var(--color-primary);
    color: white;
  }
</style>
