---
layout: layouts/base.njk
title: ""
hero:
  title: "{{SITE_NAME}}"
  tagline: "{{TAGLINE}}"
  cta: "View My Work"
  ctaLink: "/projects/"
---

{% include "components/hero.njk" %}

<section class="featured-work">
  <layout-center data-layout-max="wide">
    <layout-stack data-layout-gap="xl">
      <header class="section-header">
        <h2>Featured Work</h2>
        <a href="/projects/" class="view-all">View all projects</a>
      </header>

      <layout-grid data-layout-gap="l" data-min-width="350">
        {% for project in collections.projects | limit(3) %}
          {% include "components/project-card.njk" %}
        {% endfor %}
      </layout-grid>
    </layout-stack>
  </layout-center>
</section>

<section class="about-preview">
  <layout-center data-layout-max="narrow">
    <layout-stack data-layout-gap="l">
      <h2>About Me</h2>
      <p>I'm a designer and developer passionate about creating beautiful, functional digital experiences. With expertise in UI/UX design and front-end development, I bring ideas to life through clean code and thoughtful design.</p>
      <a href="/about/" class="button secondary">Learn more about me</a>
    </layout-stack>
  </layout-center>
</section>

<style>
  .featured-work {
    padding: var(--size-4xl) var(--size-l);
    background: var(--color-surface-sunken);
  }

  .section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--size-m);
  }

  .section-header h2 {
    font-size: var(--font-size-2xl);
  }

  .view-all {
    color: var(--color-primary);
    text-decoration: none;
    font-weight: var(--font-weight-medium);
  }

  .view-all:hover {
    text-decoration: underline;
  }

  .about-preview {
    padding: var(--size-4xl) var(--size-l);
    text-align: center;
  }

  .about-preview h2 {
    font-size: var(--font-size-2xl);
  }

  .about-preview p {
    font-size: var(--font-size-lg);
    color: var(--color-text-muted);
    max-width: 60ch;
    margin-inline: auto;
  }
</style>
