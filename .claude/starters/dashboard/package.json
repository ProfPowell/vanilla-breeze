{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "description": "{{DESCRIPTION}}",
  "type": "module",
  "scripts": {
    "dev": "npx serve -l 3000",
    "build": "echo 'No build step required'",
    "lint": "npx eslint src/",
    "test": "node --test test/"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
