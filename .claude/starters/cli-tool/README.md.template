# {{COMMAND_NAME}}

{{DESCRIPTION}}

## Installation

```bash
npm install -g {{PROJECT_NAME}}
```

Or run directly with npx:

```bash
npx {{PROJECT_NAME}}
```

## Usage

{{#IF_SIMPLE}}
```bash
{{COMMAND_NAME}} [options] <files...>
```
{{/IF_SIMPLE}}

{{#IF_MULTI_COMMAND}}
```bash
{{COMMAND_NAME}} <command> [options]
```

### Commands

| Command | Description |
|---------|-------------|
| help | Show help message |

Run `{{COMMAND_NAME}} help <command>` for command-specific help.
{{/IF_MULTI_COMMAND}}

{{#IF_INTERACTIVE}}
```bash
{{COMMAND_NAME}}
```

Follow the interactive prompts to complete the wizard.
{{/IF_INTERACTIVE}}

## Options

| Option | Description |
|--------|-------------|
| `-h, --help` | Show help message |
| `-v, --version` | Show version number |
{{#IF_ENABLE_CONFIG}}
| `-c, --config` | Path to config file |
{{/IF_ENABLE_CONFIG}}
| `--verbose` | Enable verbose output |
| `-q, --quiet` | Suppress output |

{{#IF_ENABLE_CONFIG}}
## Configuration

Create a `.{{COMMAND_NAME}}rc` file in your project root:

```json
{
  "option": "value"
}
```

Or add a `{{COMMAND_NAME}}` key to your `package.json`:

```json
{
  "{{COMMAND_NAME}}": {
    "option": "value"
  }
}
```
{{/IF_ENABLE_CONFIG}}

## Examples

{{#IF_SIMPLE}}
```bash
# Process a single file
{{COMMAND_NAME}} file.txt

# Process multiple files with verbose output
{{COMMAND_NAME}} --verbose *.js
```
{{/IF_SIMPLE}}

{{#IF_MULTI_COMMAND}}
```bash
# Show help
{{COMMAND_NAME}} help

# Run a command
{{COMMAND_NAME}} init my-project
```
{{/IF_MULTI_COMMAND}}

{{#IF_INTERACTIVE}}
```bash
# Start the wizard
{{COMMAND_NAME}}

# With verbose output
{{COMMAND_NAME}} --verbose
```
{{/IF_INTERACTIVE}}

## Development

```bash
# Install dependencies
npm install

# Link for local development
npm link

# Run tests
npm test

# Run in watch mode
npm run test:watch
```

## License

{{LICENSE}}