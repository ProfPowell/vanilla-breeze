{
  "name": "vanilla-breeze-docs-cook",
  "version": "0.1.0",
  "private": true,
  "description": "Vanilla Breeze documentation site — Cook SSG build",
  "type": "module",
  "scripts": {
    "dev": "cook dev",
    "build": "MINIFY=false cook build && pagefind",
    "build:check": "npm run build && npm run audit:templates",
    "build:prod": "NODE_ENV=production cook build && pagefind",
    "audit:templates": "node scripts/audit-unresolved-templates.js",
    "preview": "npm run build && node scripts/setup-preview.js && npx http-server dist/pages -p 3456 -c-1"
  },
  "devDependencies": {
    "cook-ssg": "^2.1.0",
    "pagefind": "^1.4.0"
  },
  "dependencies": {
    "linkedom": "^0.18.12"
  }
}
