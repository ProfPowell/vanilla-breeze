---
layout: layouts/element.njk
title: "p"
description: "The paragraph element represents a block of text, forming the basic building block of written content on the web."
elementName: p
category: native
nativeGroup: Text Content
currentPath: /docs/elements/native/p/
permalink: /docs/elements/native/p/
---

{% set defaultCode %}<p>This is a paragraph of text. Paragraphs have proper
text wrapping that avoids orphaned words at the end
of lines when possible.</p>

<p>A second paragraph shows the vertical rhythm between
adjacent paragraphs.</p>{% endset %}

{% set leadCode %}<p class="lead">This is a lead paragraph that introduces
the main content. It has larger text and muted color
to distinguish it from regular body text.</p>

<p>This is a regular paragraph that follows.</p>{% endset %}

{% set balanceCode %}<h2>Welcome to Vanilla Breeze</h2>
<p class="balance">A design system built on native HTML
elements, semantic CSS, and zero JavaScript
dependencies.</p>{% endset %}

{% set indentCode %}<p class="indent">The morning light crept through the
shutters, casting thin stripes across the wooden
floor.</p>
<p class="indent">Outside, the first birds had begun to
call. It was the kind of quiet that felt intentional.</p>
<p class="indent">She stood, finally, and crossed to the
window. The garden was still wet with dew.</p>{% endset %}

{% set justifyCode %}<p class="justify">Typography is the art and technique
of arranging type to make written language legible,
readable, and appealing when displayed.</p>{% endset %}

{% set emphasisCode %}<p>Use <strong>strong</strong> for importance.
Use <b>bold</b> for stylistic emphasis.</p>

<p>Use <em>em</em> for stress emphasis.
Use <i>italic</i> for alternative voice.</p>

<p>Price: $99.99 <small>(plus tax)</small>.</p>

<p>Search results can <mark>highlight</mark>
matching terms.</p>{% endset %}

{% set cssPropertiesCode %}p {
  text-wrap: pretty;
}

p + p {
  margin-block-start: var(--size-m);
}

p.lead {
  font-size: var(--font-size-xl);
  line-height: var(--line-height-relaxed);
  color: var(--color-text-muted);
}

p.balance {
  text-wrap: balance;
}

p.indent {
  text-indent: 1.5em;
}

p.indent + p.indent {
  margin-block-start: 0;
}

p.justify {
  text-align: justify;
  hyphens: auto;
  -webkit-hyphens: auto;
}

strong, b {
  font-weight: 600;
}

em, i {
  font-style: italic;
}

small {
  font-size: var(--font-size-sm);
}

mark {
  background: oklch(from var(--color-interactive) l c h / 0.2);
  padding-inline: var(--size-2xs);
  border-radius: var(--radius-s);
}{% endset %}

<!-- Overview Section -->
  <section>
    <h2 id="overview">Overview</h2>
    <p>The <code>&lt;p&gt;</code> element is used for blocks of text content. It automatically creates vertical spacing between adjacent paragraphs and uses modern CSS text-wrap features for optimal readability.</p>

    <browser-window src="/docs/snippets/demos/p-default.html" url="https://vanilla-breeze.dev/p/default" title="Default paragraphs" shadow></browser-window>
    <code-block language="html" label="Default paragraphs">{{ defaultCode | escape }}</code-block>
  </section>

  <!-- When to Use Section -->
  <section>
    <h2 id="when-to-use">When to Use</h2>
    <ul>
      <li><strong>Body text</strong> — For regular prose and explanatory content</li>
      <li><strong>Descriptions</strong> — Product descriptions, summaries, introductions</li>
      <li><strong>Lead paragraphs</strong> — Opening text that introduces a section</li>
      <li><strong>Form help text</strong> — Explanatory text near form controls</li>
    </ul>

    <h3>When Not to Use</h3>
    <ul>
      <li>For single words or phrases (use <code>&lt;span&gt;</code> instead)</li>
      <li>For items in a sequence (use <code>&lt;ul&gt;</code> or <code>&lt;ol&gt;</code>)</li>
      <li>For code snippets (use <code>&lt;code&gt;</code> or <code>&lt;pre&gt;</code>)</li>
      <li>For quoted text (use <code>&lt;blockquote&gt;</code>)</li>
    </ul>
  </section>

  <!-- Lead Paragraph Section -->
  <section>
    <h2 id="lead">Lead Paragraph</h2>
    <p>Use the <code>.lead</code> class to create an introductory paragraph with larger, muted text. Commonly used as the opening paragraph of an article or section.</p>

    <browser-window src="/docs/snippets/demos/p-lead.html" url="https://vanilla-breeze.dev/p/lead" title="Lead paragraph" shadow></browser-window>
    <code-block language="html" label="Lead paragraph">{{ leadCode | escape }}</code-block>
  </section>

  <!-- Balance Section -->
  <section>
    <h2 id="balance">Balanced Text</h2>
    <p>The <code>.balance</code> class applies <code>text-wrap: balance</code>, which distributes text evenly across lines. Ideal for short, centered text like hero subtitles or card descriptions.</p>

    <browser-window src="/docs/snippets/demos/p-balance.html" url="https://vanilla-breeze.dev/p/balance" title="Balanced text" shadow></browser-window>
    <code-block language="html" label="Balanced text">{{ balanceCode | escape }}</code-block>

    <p><strong>Note:</strong> Browsers limit <code>text-wrap: balance</code> to roughly 6 lines of text. For longer paragraphs, the default <code>text-wrap: pretty</code> is more appropriate.</p>
  </section>

  <!-- Indent Section -->
  <section>
    <h2 id="indent">Indented Paragraphs</h2>
    <p>The <code>.indent</code> class adds a book-style first-line indent. When consecutive indented paragraphs follow each other, the inter-paragraph margin collapses to zero, creating traditional prose formatting.</p>

    <browser-window src="/docs/snippets/demos/p-indent.html" url="https://vanilla-breeze.dev/p/indent" title="Indented paragraphs" shadow></browser-window>
    <code-block language="html" label="Indented paragraphs">{{ indentCode | escape }}</code-block>
  </section>

  <!-- Justify Section -->
  <section>
    <h2 id="justify">Justified Text</h2>
    <p>The <code>.justify</code> class aligns text to both edges and enables automatic hyphenation. This produces the polished look seen in books and newspapers. Works best in narrow columns (40-60ch).</p>

    <browser-window src="/docs/snippets/demos/p-justify.html" url="https://vanilla-breeze.dev/p/justify" title="Justified text" shadow></browser-window>
    <code-block language="html" label="Justified text">{{ justifyCode | escape }}</code-block>

    <p><strong>Accessibility note:</strong> The <code>a11y-dyslexia</code> theme automatically overrides <code>p.justify</code> to left-aligned text with no hyphenation, ensuring readability for users with dyslexia.</p>
  </section>

  <!-- Text Emphasis Section -->
  <section>
    <h2 id="text-emphasis">Text Emphasis</h2>
    <p>Paragraphs can contain inline elements for emphasis and semantic meaning.</p>

    <browser-window src="/docs/snippets/demos/p-emphasis.html" url="https://vanilla-breeze.dev/p/emphasis" title="Inline emphasis elements" shadow></browser-window>
    <code-block language="html" label="Inline emphasis elements">{{ emphasisCode | escape }}</code-block>

    <h3>Choosing the Right Element</h3>
    <table class="props-table">
      <thead>
        <tr>
          <th>Element</th>
          <th>Meaning</th>
          <th>Screen Reader</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>&lt;strong&gt;</code></td>
          <td>Importance, seriousness, urgency</td>
          <td>Announced differently</td>
        </tr>
        <tr>
          <td><code>&lt;b&gt;</code></td>
          <td>Stylistic bold (keywords, product names)</td>
          <td>Not announced</td>
        </tr>
        <tr>
          <td><code>&lt;em&gt;</code></td>
          <td>Stress emphasis (spoken differently)</td>
          <td>Announced differently</td>
        </tr>
        <tr>
          <td><code>&lt;i&gt;</code></td>
          <td>Alternative voice (terms, thoughts)</td>
          <td>Not announced</td>
        </tr>
        <tr>
          <td><code>&lt;small&gt;</code></td>
          <td>Side comments, fine print</td>
          <td>Normal reading</td>
        </tr>
        <tr>
          <td><code>&lt;mark&gt;</code></td>
          <td>Highlighted for reference</td>
          <td>Some readers announce</td>
        </tr>
      </tbody>
    </table>
  </section>

  <!-- CSS Properties Section -->
  <section>
    <h2 id="css-properties">CSS Properties</h2>

    <code-block language="css" label="Paragraph CSS styling">{{ cssPropertiesCode | escape }}</code-block>

    <h3>Variant Summary</h3>
    <table class="props-table">
      <thead>
        <tr>
          <th>Class</th>
          <th>Purpose</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>p.lead</code></td>
          <td>Larger, muted intro paragraph</td>
        </tr>
        <tr>
          <td><code>p.balance</code></td>
          <td>Even line distribution for short, centered text</td>
        </tr>
        <tr>
          <td><code>p.indent</code></td>
          <td>Book-style first-line indent with collapsed margins</td>
        </tr>
        <tr>
          <td><code>p.justify</code></td>
          <td>Justified alignment with auto-hyphenation</td>
        </tr>
      </tbody>
    </table>
  </section>

  <!-- Accessibility Section -->
  <section>
    <h2 id="accessibility">Accessibility</h2>

    <h3>Best Practices</h3>
    <ul>
      <li><strong>Semantic structure</strong> — Use paragraphs for actual paragraph content, not for layout spacing</li>
      <li><strong>Reading level</strong> — Write at an appropriate reading level for your audience</li>
      <li><strong>Line length</strong> — Keep lines to 45-75 characters for optimal readability</li>
      <li><strong>Contrast</strong> — Ensure sufficient color contrast with the background</li>
    </ul>

    <h3>Screen Reader Behavior</h3>
    <p>Screen readers treat paragraphs as distinct blocks of content. Users can navigate between paragraphs using keyboard shortcuts and hear a pause between them to indicate the break.</p>

    <h3>Dyslexia Theme</h3>
    <p>The <code>a11y-dyslexia</code> theme overrides <code>p.justify</code> to use left-aligned text and disables hyphenation. The <code>.lead</code>, <code>.balance</code>, and <code>.indent</code> classes remain unaffected since they don't interfere with readability.</p>
  </section>

  <!-- Related Elements Section -->
  <section>
    <h2 id="related">Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/headings/"><code>&lt;h1&gt;-&lt;h6&gt;</code></a> — Headings that introduce paragraph content</li>
      <li><a href="/docs/elements/native/blockquote/"><code>&lt;blockquote&gt;</code></a> — For quoted text blocks</li>
      <li><a href="/docs/elements/native/lists/"><code>&lt;ul&gt;</code>, <code>&lt;ol&gt;</code></a> — For lists of items</li>
      <li><a href="/docs/elements/native/text/">Inline semantics</a> — Elements like <code>&lt;abbr&gt;</code>, <code>&lt;cite&gt;</code>, <code>&lt;dfn&gt;</code></li>
    </ul>
  </section>
