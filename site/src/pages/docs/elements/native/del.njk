---
layout: layouts/element.njk
title: "del"
description: "The del element represents text that has been deleted from a document, typically displayed with a strikethrough and red background tint for tracking edits or showing revisions."
elementName: del
category: native
nativeGroup: Editing
currentPath: /docs/elements/native/del/
permalink: /docs/elements/native/del/
---

{% set basicDelCode %}<del>This content has been deleted.</del>{% endset %}

{% set diffVariantCode %}<del class="diff">removed code</del>{% endset %}

{% set revisionCode %}<p>The meeting will be held on <del>Tuesday</del> <ins>Wednesday</ins> at 2pm.</p>{% endset %}

{% set datetimeCode %}<del datetime="2024-01-15T10:30:00">Original content</del>{% endset %}

{% set citeCode %}<del cite="https://example.com/changelog#v2">Legacy API endpoint</del>{% endset %}

{% set codeDiffCode %}<pre><code><del class="diff">const oldVariable = "deprecated";</del>
<ins class="diff">const newVariable = "current";</ins></code></pre>{% endset %}

{% set blockDelCode %}<del>
  <p>This entire paragraph has been removed from the document.</p>
  <p>Multiple paragraphs can be deleted together.</p>
</del>{% endset %}

{% set delCssCode %}del {
  text-decoration: line-through;
  background: oklch(55% 0.2 25 / 0.1);
  text-decoration-color: oklch(55% 0.2 25);
}

del.diff {
  display: inline-block;
  padding-inline: var(--size-2xs);
  border-radius: var(--radius-s);
}{% endset %}

{% set screenReaderCode %}"deletion, This content has been deleted, end deletion"{% endset %}

<section>
    <h2>Description</h2>
    <p>The <code>&lt;del&gt;</code> element indicates text that has been removed from a document. It is semantically meaningful for document revision tracking, edit histories, and showing changes over time. VB styles it with a red <code>line-through</code> and a subtle red background tint so deletions are visually distinct from <a href="/docs/elements/native/s/"><code>&lt;s&gt;</code></a> strikethroughs.</p>
    <p>The element can optionally include <code>cite</code> and <code>datetime</code> attributes to provide machine-readable context about the deletion.</p>
  </section>

  <section>
    <h2>When to Use</h2>
    <ul>
      <li><strong>Document revisions:</strong> Track changes in legal documents, contracts, or policies</li>
      <li><strong>Edit history:</strong> Show what was changed in collaborative documents</li>
      <li><strong>Diff displays:</strong> Show removed code or text in version comparisons</li>
      <li><strong>Corrections:</strong> Indicate corrected information while preserving the original</li>
      <li><strong>Track changes:</strong> Similar to "track changes" in word processors</li>
    </ul>
    <h3>When NOT to Use</h3>
    <ul>
      <li><strong>Visual strikethrough only:</strong> Use <a href="/docs/elements/native/s/"><code>&lt;s&gt;</code></a> for content that is no longer relevant but was not editorially deleted</li>
      <li><strong>Price reductions:</strong> Use <code>&lt;s&gt;</code> unless you are tracking actual revision history</li>
    </ul>
  </section>

  <section>
    <h2>Examples</h2>

    <browser-window src="/docs/snippets/demos/del-ins-examples.html" url="del-ins-examples.html" title="del and ins examples" shadow></browser-window>

    <h3>Default</h3>
    <p>Displays with a red strikethrough and subtle red background indicating deletion.</p>
    <code-block language="html" show-lines label="Basic del element">{{ basicDelCode | escape }}</code-block>

    <h3>.diff</h3>
    <p>Enhanced styling for diff displays with padding and rounded corners, ideal for code comparisons.</p>
    <code-block language="html" show-lines label="Diff styling variant">{{ diffVariantCode | escape }}</code-block>

    <h3>Document Revision</h3>
    <p>Pair <code>&lt;del&gt;</code> with <a href="/docs/elements/native/ins/"><code>&lt;ins&gt;</code></a> to show before-and-after edits in running text.</p>
    <code-block language="html" show-lines label="Revision with del + ins">{{ revisionCode | escape }}</code-block>

    <h3>Code Diff Display</h3>
    <p>Use the <code>.diff</code> class on both <code>&lt;del&gt;</code> and <code>&lt;ins&gt;</code> for compact, pill-shaped diff lines.</p>
    <code-block language="html" show-lines label="Code diff with .diff class">{{ codeDiffCode | escape }}</code-block>

    <h3>Block-Level Deletion</h3>
    <p>The <code>&lt;del&gt;</code> element can wrap block content for multi-paragraph deletions.</p>
    <code-block language="html" show-lines label="Block-level del">{{ blockDelCode | escape }}</code-block>
  </section>

  <section>
    <h2>Key Attributes</h2>
    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
          <th>Example</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>datetime</code></td>
          <td>Date and time of the deletion in ISO 8601 format. Not visible to users but available to scripts and assistive technology.</td>
          <td><code>datetime="2024-01-15T10:30:00"</code></td>
        </tr>
        <tr>
          <td><code>cite</code></td>
          <td>URL to a document explaining the change. Not rendered by the browser but useful for audit trails.</td>
          <td><code>cite="https://example.com/changelog"</code></td>
        </tr>
      </tbody>
    </table>

    <code-block language="html" show-lines label="Del with datetime attribute">{{ datetimeCode | escape }}</code-block>
    <code-block language="html" show-lines label="Del with cite attribute">{{ citeCode | escape }}</code-block>
  </section>

  <section>
    <h2>del vs s</h2>
    <p>Both elements produce a strikethrough, but they carry different semantics and VB gives them different visual treatments to match:</p>
    <table class="props-table">
      <thead>
        <tr>
          <th>Element</th>
          <th>Meaning</th>
          <th>VB Styling</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><a href="/docs/elements/native/del/"><code>&lt;del&gt;</code></a></td>
          <td>Editorial change &mdash; text was removed from the document</td>
          <td>Red strikethrough + red background tint</td>
        </tr>
        <tr>
          <td><a href="/docs/elements/native/s/"><code>&lt;s&gt;</code></a></td>
          <td>No longer accurate/relevant &mdash; not an edit</td>
          <td>Muted strikethrough only (no background)</td>
        </tr>
      </tbody>
    </table>
    <p>Use <code>&lt;del&gt;</code> when the content was editorially removed (contracts, changelogs, diffs). Use <code>&lt;s&gt;</code> for prices, expired offers, or outdated info.</p>
  </section>

  <section>
    <h2>CSS Reference</h2>
    <code-block language="css" show-lines label="del CSS">{{ delCssCode | escape }}</code-block>
    <p>The red background at 10% opacity provides a subtle tint that works across light and dark themes. The <code>.diff</code> variant adds inline-block display with padding for pill-shaped diff tokens.</p>
  </section>

  <section>
    <h2>Accessibility</h2>
    <ul>
      <li><strong>Screen reader announcement:</strong> Most screen readers announce "deletion" before reading deleted content and "end deletion" after</li>
      <li><strong>Dual visual cues:</strong> Strikethrough and background color together ensure visibility, including for users who may not perceive color</li>
      <li><strong>Don't hide important info:</strong> Ensure any still-visible deleted content remains accessible</li>
      <li><strong>Datetime attribute:</strong> Provides machine-readable context about when changes occurred</li>
    </ul>

    <h3>Screen Reader Behavior</h3>
    <p>With proper screen reader settings, users will hear something like:</p>
    <code-block language="html" show-lines label="Screen reader announcement">{{ screenReaderCode | escape }}</code-block>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/ins/"><code>&lt;ins&gt;</code></a> &mdash; Companion element for inserted text (use together for document revisions)</li>
      <li><a href="/docs/elements/native/s/"><code>&lt;s&gt;</code></a> &mdash; Strikethrough for content that is no longer accurate, without editorial semantics</li>
      <li><a href="/docs/elements/native/u/"><code>&lt;u&gt;</code></a> &mdash; Wavy red underline for annotations (see VB underline styles comparison)</li>
      <li><a href="/docs/elements/native/mark/"><code>&lt;mark&gt;</code></a> &mdash; Highlighted text for reference (background tint without strikethrough)</li>
    </ul>
  </section>
