---
layout: layouts/element.njk
title: "thead"
description: "The thead element groups header rows in a table. Vanilla Breeze styles it with a raised background and a medium bottom border to visually separate column headers from data."
elementName: thead
category: native
nativeGroup: Tables
currentPath: /docs/elements/native/thead/
permalink: /docs/elements/native/thead/
---

{% set basicTheadCode %}<table>
  <caption>Quarterly Report</caption>
  <thead>
    <tr>
      <th scope="col">Product</th>
      <th scope="col" data-numeric>Q1</th>
      <th scope="col" data-numeric>Q2</th>
    </tr>
  </thead>
  <tbody>
    <!-- Data rows -->
  </tbody>
  <tfoot>
    <tr>
      <td>Total</td>
      <td data-numeric>$27,600</td>
      <td data-numeric>$31,800</td>
    </tr>
  </tfoot>
</table>{% endset %}

{% set stickyCode %}<table data-sticky="header">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Product</th>
      <th scope="col" data-numeric>Price</th>
    </tr>
  </thead>
  <tbody>
    <!-- Many rows of data -->
  </tbody>
</table>{% endset %}

{% set theadCss %}/* VB default thead styles */
thead th {
  background: var(--color-surface-raised);
  border-block-end: var(--border-width-medium)
    solid var(--color-border);
}

/* Sticky header via data attribute */
table[data-sticky="header"] thead th {
  position: sticky;
  inset-block-start: 0;
  z-index: 1;
}{% endset %}

  <section>
    <h2>Description</h2>
    <p>The <code>&lt;thead&gt;</code> element contains one or more <a href="/docs/elements/native/tr/"><code>&lt;tr&gt;</code></a> elements that define the header row(s) of a table. It must appear after any <code>&lt;caption&gt;</code> or <code>&lt;colgroup&gt;</code> elements and before <a href="/docs/elements/native/tbody/"><code>&lt;tbody&gt;</code></a> and <a href="/docs/elements/native/tfoot/"><code>&lt;tfoot&gt;</code></a>.</p>
    <p>Vanilla Breeze styles <code>&lt;thead&gt;</code> header cells with a raised background (<code>var(--color-surface-raised)</code>) and a thicker bottom border (<code>var(--border-width-medium)</code>) to visually separate headers from data rows.</p>
  </section>

  <section>
    <h2>When to Use</h2>
    <ul>
      <li><strong>Every data table:</strong> All tables with meaningful data should have thead to define column headers</li>
      <li><strong>Multiple header rows:</strong> When columns have grouped headers or sub-headers</li>
      <li><strong>Printed tables:</strong> Headers repeat on each page when printing long tables</li>
      <li><strong>Sticky headers:</strong> Use <code>data-sticky="header"</code> on the table so headers remain visible while scrolling</li>
    </ul>
  </section>

  <section>
    <h2>Examples</h2>

    <h3>Basic Table with thead</h3>
    <p>A complete table using thead with <a href="/docs/elements/native/th/"><code>&lt;th scope="col"&gt;</code></a> for column headers, alongside <a href="/docs/elements/native/tbody/"><code>&lt;tbody&gt;</code></a> and <a href="/docs/elements/native/tfoot/"><code>&lt;tfoot&gt;</code></a>.</p>
    <browser-window src="/docs/snippets/demos/table-basic.html" url="https://vanilla-breeze.dev/table/basic" title="Basic table with thead, tbody, and tfoot" shadow></browser-window>
    <code-block language="html" label="Basic thead example">{{ basicTheadCode | escape }}</code-block>

    <h3>Sticky Header</h3>
    <p>Add <code>data-sticky="header"</code> to the <code>&lt;table&gt;</code> element so the header row stays visible as the user scrolls through long tables. VB applies <code>position: sticky</code>, <code>inset-block-start: 0</code>, and <code>z-index: 1</code> to the <code>thead th</code> cells.</p>
    <code-block language="html" label="Sticky header">{{ stickyCode | escape }}</code-block>

    <h3>Scope Attribute</h3>
    <p>The <code>scope</code> attribute on <code>&lt;th&gt;</code> tells assistive technologies which cells the header relates to. See the shared demo for headers using <code>scope="col"</code> and <code>scope="row"</code>.</p>
    <browser-window src="/docs/snippets/demos/table-scope.html" url="https://vanilla-breeze.dev/table/scope" title="Table with row and column scope" shadow></browser-window>
  </section>

  <section>
    <h2>CSS Reference</h2>
    <p>Vanilla Breeze applies these styles to <code>&lt;th&gt;</code> elements within thead:</p>

    <code-block language="css" label="VB thead CSS">{{ theadCss | escape }}</code-block>

    <table class="props-table">
      <thead>
        <tr>
          <th scope="col">Property</th>
          <th scope="col">Value</th>
          <th scope="col">Purpose</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>background</code></td>
          <td><code>var(--color-surface-raised)</code></td>
          <td>Subtle background to distinguish headers from data</td>
        </tr>
        <tr>
          <td><code>border-block-end</code></td>
          <td><code>var(--border-width-medium) solid var(--color-border)</code></td>
          <td>Thicker border separating header from body</td>
        </tr>
        <tr>
          <td><code>position: sticky</code></td>
          <td>Via <code>data-sticky="header"</code></td>
          <td>Header stays fixed at the top when scrolling</td>
        </tr>
        <tr>
          <td><code>z-index</code></td>
          <td><code>1</code></td>
          <td>Ensures sticky header paints above body rows</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Accessibility</h2>
    <ul>
      <li><strong>Structural grouping:</strong> Helps assistive technologies distinguish header rows from data rows</li>
      <li><strong>Always use scope:</strong> Set <code>scope="col"</code> on each <a href="/docs/elements/native/th/"><code>&lt;th&gt;</code></a> inside thead so screen readers announce the header for each column</li>
      <li><strong>Grouped headers:</strong> Use <code>scope="colgroup"</code> with <code>colspan</code> for multi-column header groups</li>
      <li><strong>Implicit role:</strong> thead has an implicit ARIA role of <code>rowgroup</code></li>
      <li><strong>Repeated headers:</strong> When printing, thead content can repeat on each page</li>
    </ul>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/table/"><code>&lt;table&gt;</code></a> - Parent container for thead</li>
      <li><a href="/docs/elements/native/tr/"><code>&lt;tr&gt;</code></a> - Table rows within thead</li>
      <li><a href="/docs/elements/native/th/"><code>&lt;th&gt;</code></a> - Header cells (always use in thead with <code>scope</code>)</li>
      <li><a href="/docs/elements/native/tbody/"><code>&lt;tbody&gt;</code></a> - Data row group (follows thead)</li>
      <li><a href="/docs/elements/native/tfoot/"><code>&lt;tfoot&gt;</code></a> - Footer row group for summaries and totals</li>
      <li><a href="/docs/elements/components/data-table/"><code>&lt;data-table&gt;</code></a> - Enhanced table component with sorting, filtering, and pagination</li>
    </ul>
  </section>
