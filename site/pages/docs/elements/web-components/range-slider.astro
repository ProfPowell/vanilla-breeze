---
/**
 * Range Slider Documentation
 *
 * Range input with value bubble and datalist labels.
 */
import ElementLayout from '../../../../layouts/ElementLayout.astro';

const basicCode = `<input type="range" min="0" max="100" value="50"
  data-range data-bubble>`;

const prefixCode = `<input type="range" min="0" max="1000" step="100"
  value="500" data-range data-bubble data-prefix="$">`;

const datalistCode = `<input type="range" min="0" max="1000" step="100"
  list="prices" data-range data-bubble data-prefix="$">
<datalist id="prices">
  <option value="0" label="$0">
  <option value="250" label="$250">
  <option value="500" label="$500">
  <option value="750" label="$750">
  <option value="1000" label="$1K">
</datalist>`;

const suffixCode = `<input type="range" min="60" max="85" value="72"
  data-range data-bubble data-suffix="°F">`;

const cssCode = `/* Custom track color */
.range-wrapper input[type="range"] \{
  background: linear-gradient(
    to right,
    var(--color-success) var(--range-pct, 0%),
    var(--color-surface-raised) var(--range-pct, 0%)
  );
\}

/* Custom thumb */
.range-wrapper input[type="range"]::-webkit-slider-thumb \{
  background: var(--color-success);
\}`;
---

<ElementLayout
  title="Range Slider"
  description="Enhanced range input with floating value bubble, prefix/suffix, and datalist tick labels."
  elementName="data-range"
  category="component"
  currentPath="/docs/elements/web-components/range-slider/"
>
  <section>
    <h2>Overview</h2>
    <p>Add <code>data-range</code> to an <code>&lt;input type="range"&gt;</code> for styled cross-browser track and thumb. Add <code>data-bubble</code> for a floating value display. Pair with a <code>&lt;datalist&gt;</code> for tick labels.</p>

    <browser-window
      src="/docs/examples/demos/range-basic.html"
      url="https://vanilla-breeze.dev/range-slider"
      title="Range Slider Demo"
      shadow>
    </browser-window>
  </section>

  <section>
    <h2>Basic with Bubble</h2>
    <p>The value bubble floats above the thumb and updates in real-time.</p>

    <div class="example">
      <form-field>
        <label for="demo-vol">Volume</label>
        <input type="range" id="demo-vol" min="0" max="100" value="50" data-range data-bubble data-suffix="%">
      </form-field>
    </div>

    <code-block language="html" show-lines label="Basic range with bubble">{basicCode}</code-block>
  </section>

  <section>
    <h2>Prefix &amp; Suffix</h2>
    <p>Use <code>data-prefix</code> and <code>data-suffix</code> to add context to the displayed value.</p>

    <div class="example">
      <form-field>
        <label for="demo-price">Budget</label>
        <input type="range" id="demo-price" min="0" max="1000" step="100" value="500" data-range data-bubble data-prefix="$">
      </form-field>
    </div>

    <code-block language="html" show-lines label="With prefix">{prefixCode}</code-block>
    <code-block language="html" show-lines label="With suffix">{suffixCode}</code-block>
  </section>

  <section>
    <h2>Datalist Labels</h2>
    <p>Pair with a <code>&lt;datalist&gt;</code> to show tick mark labels below the slider. The <code>label</code> attribute on each <code>&lt;option&gt;</code> is used for display.</p>

    <code-block language="html" show-lines label="With datalist labels">{datalistCode}</code-block>
  </section>

  <section>
    <h2>Attributes</h2>
    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>data-range</code></td>
          <td>Enable range enhancement (cross-browser track/thumb styling and fill).</td>
        </tr>
        <tr>
          <td><code>data-bubble</code></td>
          <td>Show floating value bubble above the thumb.</td>
        </tr>
        <tr>
          <td><code>data-prefix</code></td>
          <td>Text before the value (e.g., <code>"$"</code>).</td>
        </tr>
        <tr>
          <td><code>data-suffix</code></td>
          <td>Text after the value (e.g., <code>"%"</code>, <code>"°F"</code>).</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Styling</h2>
    <p>The track fill uses a CSS custom property <code>--range-pct</code> set by JS. Override the gradient to change colors.</p>

    <code-block language="css" show-lines label="Custom colors">{cssCode}</code-block>
  </section>

  <section>
    <h2>Progressive Enhancement</h2>
    <ul>
      <li><strong>No JS:</strong> Native range input with browser-default styling. Fully functional.</li>
      <li><strong>With JS:</strong> Cross-browser styled track, value bubble, and datalist labels.</li>
    </ul>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/input/"><code>&lt;input&gt;</code></a> &mdash; Native input element</li>
      <li><a href="/docs/elements/native/datalist/"><code>&lt;datalist&gt;</code></a> &mdash; Tick mark suggestions</li>
      <li><a href="/docs/elements/custom-elements/form-field/"><code>&lt;form-field&gt;</code></a> &mdash; Form field wrapper</li>
    </ul>
  </section>
</ElementLayout>
