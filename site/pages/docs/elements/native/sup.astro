---
/**
 * Superscript (<sup>) Element Documentation
 *
 * The sup element represents superscript text.
 */
import ElementLayout from '../../../../layouts/ElementLayout.astro';

// Code examples as template strings
const basicSupCode = `<p>E = mc<sup>2</sup></p>`;

const exponentCode = `<p>
  The area of a circle is <var>A</var> = <var>π</var><var>r</var><sup>2</sup>,
  where <var>r</var> is the radius.
</p>`;

const ordinalCode = `<p>
  The 1<sup>st</sup> place winner will receive a gold medal,
  the 2<sup>nd</sup> place a silver, and 3<sup>rd</sup> place a bronze.
</p>`;

const footnoteCode = `<p>
  According to recent studies<sup><a href="#ref1">[1]</a></sup>,
  this approach is more effective<sup><a href="#ref2">[2]</a></sup>.
</p>`;

const trademarkCode = `<p>
  Welcome to Awesome Product<sup>TM</sup> — the best solution<sup>®</sup> for your needs.
</p>`;
---

<ElementLayout
  title="sup"
  description="Represents superscript text, typically displayed higher and smaller."
  elementName="sup"
  category="native"
  nativeGroup="Text Semantics"
  currentPath="/docs/elements/native/sup/"
>
  <section>
    <h2>Description</h2>
    <p>The <code>&lt;sup&gt;</code> element specifies inline text which should be displayed as superscript for solely typographical reasons. Superscripts are typically rendered with a raised baseline using smaller text. Common uses include exponents in mathematical expressions, ordinal numbers (like 1st, 2nd), footnote references, and trademark/registered symbols.</p>
    <p>Use <code>&lt;sup&gt;</code> only for typographical conventions, not for purely presentational purposes. For styling purposes, use CSS (<code>vertical-align: super</code>).</p>
  </section>

  <section>
    <h2>Basic Example</h2>
    <div class="example">
      <p>E = mc<sup>2</sup></p>
    </div>
    <code-block language="html" label="Basic sup example">{basicSupCode}</code-block>
  </section>

  <section>
    <h2>Mathematical Exponents</h2>
    <p>Superscripts are essential for mathematical notation:</p>
    <div class="example">
      <p>
        The area of a circle is <var>A</var> = <var>π</var><var>r</var><sup>2</sup>,
        where <var>r</var> is the radius.
      </p>
    </div>
    <code-block language="html" label="Math exponents">{exponentCode}</code-block>
  </section>

  <section>
    <h2>Ordinal Numbers</h2>
    <p>Superscripts for ordinal indicators:</p>
    <div class="example">
      <p>
        The 1<sup>st</sup> place winner will receive a gold medal,
        the 2<sup>nd</sup> place a silver, and 3<sup>rd</sup> place a bronze.
      </p>
    </div>
    <code-block language="html" label="Ordinal numbers">{ordinalCode}</code-block>
  </section>

  <section>
    <h2>Footnote References</h2>
    <p>Superscripts are commonly used for footnote and citation references:</p>
    <div class="example">
      <p>
        According to recent studies<sup><a href="#ref1">[1]</a></sup>,
        this approach is more effective<sup><a href="#ref2">[2]</a></sup>.
      </p>
    </div>
    <code-block language="html" label="Footnote references">{footnoteCode}</code-block>
  </section>

  <section>
    <h2>Trademark Symbols</h2>
    <p>Trademark and registered symbols are often superscripted:</p>
    <div class="example">
      <p>
        Welcome to Awesome Product<sup>TM</sup> — the best solution<sup>®</sup> for your needs.
      </p>
    </div>
    <code-block language="html" label="Trademark symbols">{trademarkCode}</code-block>
  </section>

  <section>
    <h2>Attributes</h2>
    <p>This element supports <a href="/docs/elements/native/states/">global attributes</a>.</p>
  </section>

  <section>
    <h2>Accessibility</h2>
    <p>Screen readers may not announce superscript formatting. For mathematical notation or footnotes where the superscript is meaningful, ensure the context is clear or provide alternative descriptions. Consider using MathML for complex mathematical expressions.</p>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/sub/"><code>&lt;sub&gt;</code></a> - For subscript text</li>
      <li><a href="/docs/elements/native/var/"><code>&lt;var&gt;</code></a> - For mathematical variables</li>
      <li><a href="/docs/elements/native/math/"><code>&lt;math&gt;</code></a> - For complex mathematical expressions (MathML)</li>
    </ul>
  </section>
</ElementLayout>
