---
/**
 * Ruby Text (<rt>) Element Documentation
 *
 * The rt element specifies the ruby text component of a ruby annotation.
 */
import ElementLayout from '../../../../layouts/ElementLayout.astro';

// Code examples as template strings
const basicRtCode = `<ruby>
  漢字 <rt>かんじ</rt>
</ruby>`;

const withRpCode = `<ruby>
  東京 <rp>(</rp><rt>とうきょう</rt><rp>)</rp>
</ruby>`;

const individualRtCode = `<ruby>
  漢 <rt>かん</rt>
  字 <rt>じ</rt>
</ruby>`;

const styledRtCode = `<style>
  rt {
    font-size: 0.5em;
    color: #666;
    font-style: normal;
  }
</style>

<ruby>
  日本語 <rt>にほんご</rt>
</ruby>`;

const pinyinRtCode = `<ruby>
  中文 <rt>zhōngwén</rt>
</ruby>`;
---

<ElementLayout
  title="rt"
  description="Specifies the ruby text (annotation) in a ruby annotation."
  elementName="rt"
  category="native"
  nativeGroup="Text Semantics"
  currentPath="/docs/elements/native/rt/"
>
  <section>
    <h2>Description</h2>
    <p>The <code>&lt;rt&gt;</code> element specifies the ruby text component of a ruby annotation. This is the small text that appears above (or beside) the base text to provide pronunciation or other annotation. It must be contained within a <a href="/docs/elements/native/ruby/"><code>&lt;ruby&gt;</code></a> element.</p>
  </section>

  <section>
    <h2>Basic Example</h2>
    <div class="example">
      <ruby>
        漢字 <rt>かんじ</rt>
      </ruby>
    </div>
    <code-block language="html" label="Basic rt example">{basicRtCode}</code-block>
  </section>

  <section>
    <h2>With Fallback Parentheses</h2>
    <p>Use <code>&lt;rp&gt;</code> elements to provide fallback parentheses for browsers that don't support ruby:</p>
    <div class="example">
      <ruby>
        東京 <rp>(</rp><rt>とうきょう</rt><rp>)</rp>
      </ruby>
    </div>
    <code-block language="html" label="RT with fallback">{withRpCode}</code-block>
  </section>

  <section>
    <h2>Individual Character Annotations</h2>
    <p>Each character can have its own ruby text:</p>
    <div class="example">
      <ruby>
        漢 <rt>かん</rt>
        字 <rt>じ</rt>
      </ruby>
    </div>
    <code-block language="html" label="Individual annotations">{individualRtCode}</code-block>
  </section>

  <section>
    <h2>Pinyin Annotations</h2>
    <p>Ruby text is also used for Chinese pinyin:</p>
    <div class="example">
      <ruby>
        中文 <rt>zhōngwén</rt>
      </ruby>
    </div>
    <code-block language="html" label="Pinyin annotation">{pinyinRtCode}</code-block>
  </section>

  <section>
    <h2>Styling Ruby Text</h2>
    <p>The <code>&lt;rt&gt;</code> element can be styled with CSS:</p>
    <code-block language="css" label="Styled rt">{styledRtCode}</code-block>
    <p>Common styling adjustments include font size, color, and position.</p>
  </section>

  <section>
    <h2>Attributes</h2>
    <p>This element supports <a href="/docs/elements/native/states/">global attributes</a>.</p>
  </section>

  <section>
    <h2>Accessibility</h2>
    <p>Screen readers handle ruby text differently. Some may read the base text followed by the ruby text, others may skip the ruby text or provide options for users to hear it. The ruby text is valuable for language learners and those who need pronunciation assistance.</p>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/ruby/"><code>&lt;ruby&gt;</code></a> - Parent element for ruby annotations</li>
      <li><a href="/docs/elements/native/rp/"><code>&lt;rp&gt;</code></a> - Fallback parentheses</li>
    </ul>
  </section>
</ElementLayout>
