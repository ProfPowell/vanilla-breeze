---
/**
 * Bidirectional Override (<bdo>) Element Documentation
 *
 * The bdo element overrides the current text direction.
 */
import ElementLayout from '../../../../layouts/ElementLayout.astro';

// Code examples as template strings
const basicBdoCode = `<p>Normal text: Hello World</p>
<p>Reversed: <bdo dir="rtl">Hello World</bdo></p>`;

const ltrOverrideCode = `<p dir="rtl">
  This paragraph is right-to-left, but
  <bdo dir="ltr">this text is forced left-to-right</bdo>.
</p>`;

const rtlOverrideCode = `<p>
  The Hebrew word for "peace" is <bdo dir="rtl">שלום</bdo>.
</p>`;

const nestedBdoCode = `<p>
  <bdo dir="rtl">
    Outer RTL: Hello
    <bdo dir="ltr">Inner LTR: World</bdo>
    Back to RTL
  </bdo>
</p>`;
---

<ElementLayout
  title="bdo"
  description="Overrides the current text direction."
  elementName="bdo"
  category="native"
  nativeGroup="Text Semantics"
  currentPath="/docs/elements/native/bdo/"
>
  <section>
    <h2>Description</h2>
    <p>The <code>&lt;bdo&gt;</code> (Bidirectional Text Override) element overrides the current directionality of text, causing the text within to be rendered in a specified direction. This element forces text direction regardless of the Unicode Bidirectional Algorithm.</p>
    <p>Use <code>&lt;bdo&gt;</code> when you need to explicitly control text direction for display purposes. For isolating text without forcing direction, use <a href="/docs/elements/native/bdi/"><code>&lt;bdi&gt;</code></a> instead.</p>
  </section>

  <section>
    <h2>Basic Example</h2>
    <div class="example">
      <p>Normal text: Hello World</p>
      <p>Reversed: <bdo dir="rtl">Hello World</bdo></p>
    </div>
    <code-block language="html" label="Basic bdo example">{basicBdoCode}</code-block>
  </section>

  <section>
    <h2>LTR Override</h2>
    <p>Force left-to-right direction within an RTL context:</p>
    <div class="example">
      <p dir="rtl">
        This paragraph is right-to-left, but
        <bdo dir="ltr">this text is forced left-to-right</bdo>.
      </p>
    </div>
    <code-block language="html" label="LTR override">{ltrOverrideCode}</code-block>
  </section>

  <section>
    <h2>RTL Override</h2>
    <p>Force right-to-left direction for specific text:</p>
    <div class="example">
      <p>
        The Hebrew word for "peace" is <bdo dir="rtl">שלום</bdo>.
      </p>
    </div>
    <code-block language="html" label="RTL override">{rtlOverrideCode}</code-block>
  </section>

  <section>
    <h2>Nested Overrides</h2>
    <p>BDO elements can be nested to create complex directional layouts:</p>
    <div class="example">
      <p>
        <bdo dir="rtl">
          Outer RTL: Hello
          <bdo dir="ltr">Inner LTR: World</bdo>
          Back to RTL
        </bdo>
      </p>
    </div>
    <code-block language="html" label="Nested bdo">{nestedBdoCode}</code-block>
  </section>

  <section>
    <h2>Attributes</h2>
    <table>
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>dir</code></td>
          <td><strong>Required.</strong> The text direction: <code>ltr</code> (left-to-right), <code>rtl</code> (right-to-left)</td>
        </tr>
      </tbody>
    </table>
    <p>This element also supports <a href="/docs/elements/native/states/">global attributes</a>.</p>
  </section>

  <section>
    <h2>Accessibility</h2>
    <p>The <code>&lt;bdo&gt;</code> element affects visual presentation only. Screen readers typically read text in its logical order, not the visual order. Be cautious when using <code>&lt;bdo&gt;</code> purely for visual effects, as it may cause confusion for users who cannot see the rendered result.</p>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/bdi/"><code>&lt;bdi&gt;</code></a> - Isolates text without forcing direction</li>
    </ul>
  </section>
</ElementLayout>
