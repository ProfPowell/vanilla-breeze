---
/**
 * Table Column (<col>) Element Documentation
 *
 * The col element defines properties for a single column or a span of columns
 * within a colgroup. It allows fine-grained control over column widths and styles.
 */
import ElementLayout from '../../../../layouts/ElementLayout.astro';

// Code examples as template strings
const basicColCode = `<table>
  <caption>Team Schedule</caption>
  <colgroup>
    <col style="width: 150px;" />
    <col style="width: auto;" />
    <col style="width: 100px;" />
  </colgroup>
  <thead>
    <!-- Headers -->
  </thead>
  <tbody>
    <!-- Data rows -->
  </tbody>
</table>`;

const spanAttributeCode = `<colgroup>
  <col style="width: 25%;" />
  <col span="4" style="width: 18.75%;" />
</colgroup>`;

const columnHighlightCode = `<colgroup>
  <col />
  <col />
  <col style="background: oklch(95% 0.05 140);" />
  <col />
</colgroup>`;

const cssCellTargetingCode = `/* Using CSS to style cells in a column */
table td:nth-child(3),
table th:nth-child(3) {
  text-align: end;
  font-weight: bold;
}`;

const dataNumericCode = `<th scope="col" data-numeric>Amount</th>
<td data-numeric>$1,234.56</td>`;
---

<ElementLayout
  title="col"
  description="The col element defines properties for a single column or a span of columns within a colgroup. It allows fine-grained control over column widths and styles."
  elementName="col"
  category="native"
  nativeGroup="Tables"
  currentPath="/docs/elements/native/col/"
>
  <section>
    <h2>Description</h2>
    <p>The <code>&lt;col&gt;</code> element is a void element (self-closing) that must be placed inside a <a href="/docs/elements/native/colgroup/"><code>&lt;colgroup&gt;</code></a> element. It represents one or more columns in the table and allows you to apply styles to all cells in that column without repeating styles on each cell.</p>
    <p>This element is particularly useful for setting consistent column widths and applying background colors to entire columns.</p>
  </section>

  <section>
    <h2>When to Use</h2>
    <ul>
      <li><strong>Fixed column widths:</strong> Set specific widths for columns that should not auto-size</li>
      <li><strong>Column highlighting:</strong> Apply background colors to draw attention to specific columns</li>
      <li><strong>Proportional sizing:</strong> Define percentage-based widths for responsive tables</li>
      <li><strong>Multiple columns:</strong> Use <code>span</code> to apply the same properties to several columns</li>
    </ul>
  </section>

  <section>
    <h2>Basic Example</h2>
    <p>Define individual column properties within a colgroup.</p>

    <div class="example">
      <table>
        <caption>Team Schedule</caption>
        <colgroup>
          <col style="width: 150px;" />
          <col style="width: auto;" />
          <col style="width: 100px;" />
        </colgroup>
        <thead>
          <tr>
            <th scope="col">Date</th>
            <th scope="col">Task</th>
            <th scope="col">Status</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>2024-01-15</td>
            <td>Design review meeting</td>
            <td>Complete</td>
          </tr>
          <tr>
            <td>2024-01-16</td>
            <td>Development sprint planning</td>
            <td>In Progress</td>
          </tr>
          <tr>
            <td>2024-01-17</td>
            <td>Client presentation</td>
            <td>Scheduled</td>
          </tr>
        </tbody>
      </table>
    </div>

    <code-block language="html" show-lines label="Table with col elements for column widths">{basicColCode}</code-block>
  </section>

  <section>
    <h2>Using the span Attribute</h2>
    <p>The <code>span</code> attribute applies the same properties to multiple consecutive columns.</p>

    <div class="example">
      <table>
        <caption>Quarterly Results</caption>
        <colgroup>
          <col style="width: 25%;" />
          <col span="4" style="width: 18.75%;" />
        </colgroup>
        <thead>
          <tr>
            <th scope="col">Metric</th>
            <th scope="col" data-numeric>Q1</th>
            <th scope="col" data-numeric>Q2</th>
            <th scope="col" data-numeric>Q3</th>
            <th scope="col" data-numeric>Q4</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Revenue</td>
            <td data-numeric>$150K</td>
            <td data-numeric>$175K</td>
            <td data-numeric>$200K</td>
            <td data-numeric>$225K</td>
          </tr>
          <tr>
            <td>Expenses</td>
            <td data-numeric>$90K</td>
            <td data-numeric>$95K</td>
            <td data-numeric>$110K</td>
            <td data-numeric>$120K</td>
          </tr>
        </tbody>
      </table>
    </div>

    <code-block language="html" show-lines label="Using span attribute for multiple columns">{spanAttributeCode}</code-block>
  </section>

  <section>
    <h2>Column Highlighting</h2>
    <p>Use background colors to highlight specific columns of importance.</p>

    <div class="example">
      <table>
        <caption>Price Comparison (Recommended plan highlighted)</caption>
        <colgroup>
          <col />
          <col />
          <col style="background: oklch(95% 0.05 140);" />
          <col />
        </colgroup>
        <thead>
          <tr>
            <th scope="col">Feature</th>
            <th scope="col">Starter</th>
            <th scope="col">Professional</th>
            <th scope="col">Enterprise</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <th scope="row">Price/month</th>
            <td>$9</td>
            <td>$29</td>
            <td>$99</td>
          </tr>
          <tr>
            <th scope="row">Projects</th>
            <td>3</td>
            <td>Unlimited</td>
            <td>Unlimited</td>
          </tr>
          <tr>
            <th scope="row">Team members</th>
            <td>1</td>
            <td>10</td>
            <td>Unlimited</td>
          </tr>
          <tr>
            <th scope="row">Storage</th>
            <td>5 GB</td>
            <td>50 GB</td>
            <td>500 GB</td>
          </tr>
        </tbody>
      </table>
    </div>

    <code-block language="html" show-lines label="Column highlighting with background color">{columnHighlightCode}</code-block>
  </section>

  <section>
    <h2>Attributes</h2>

    <table class="props-table">
      <thead>
        <tr>
          <th scope="col">Attribute</th>
          <th scope="col">Value</th>
          <th scope="col">Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>span</code></td>
          <td>Positive integer (default: 1)</td>
          <td>Number of consecutive columns this element spans</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Styling Limitations</h2>
    <p>Like <a href="/docs/elements/native/colgroup/"><code>&lt;colgroup&gt;</code></a>, the <code>&lt;col&gt;</code> element has limited CSS support:</p>

    <ul>
      <li><strong>Supported:</strong> <code>background</code>, <code>width</code>, <code>border</code> (with collapsed borders), <code>visibility: collapse</code></li>
      <li><strong>Not supported:</strong> <code>padding</code>, <code>text-align</code>, <code>color</code>, <code>font</code> properties</li>
    </ul>

    <p>For unsupported properties, target cells directly:</p>

    <code-block language="css" show-lines label="CSS targeting cells in a column">{cssCellTargetingCode}</code-block>

    <p>Or use the <code>data-numeric</code> attribute for right-aligned numeric columns:</p>

    <code-block language="html" show-lines label="Using data-numeric for right-aligned columns">{dataNumericCode}</code-block>
  </section>

  <section>
    <h2>Accessibility</h2>
    <ul>
      <li><strong>Decorative element:</strong> The <code>&lt;col&gt;</code> element is primarily for styling and does not convey semantic meaning</li>
      <li><strong>Header associations:</strong> Proper column relationships are established through <a href="/docs/elements/native/th/"><code>&lt;th scope="col"&gt;</code></a>, not through <code>&lt;col&gt;</code></li>
      <li><strong>Color contrast:</strong> If using background colors, ensure text remains readable with sufficient contrast</li>
    </ul>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/colgroup/"><code>&lt;colgroup&gt;</code></a> - Parent container for col elements</li>
      <li><a href="/docs/elements/native/table/"><code>&lt;table&gt;</code></a> - The table container</li>
      <li><a href="/docs/elements/native/th/"><code>&lt;th&gt;</code></a> - Column headers that provide semantic meaning</li>
      <li><a href="/docs/elements/native/td/"><code>&lt;td&gt;</code></a> - Data cells within columns</li>
    </ul>
  </section>
</ElementLayout>
