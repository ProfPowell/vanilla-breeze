---
import DocsLayout from '../../../../layouts/DocsLayout.astro';

const simpleBreadcrumbCode = `<nav class="breadcrumb" aria-label="Breadcrumb">
  <ol>
    <li><a href="/">Home</a></li>
    <li><a href="/docs/">Documentation</a></li>
    <li><a href="/docs/patterns/">Patterns</a></li>
    <li><span aria-current="page">Breadcrumb</span></li>
  </ol>
</nav>`;

const homeIconCode = `<nav class="breadcrumb" aria-label="Breadcrumb">
  <ol>
    <li>
      <a href="/">
        <icon-wc name="home" size="sm" aria-hidden="true"></icon-wc>
        <span class="visually-hidden">Home</span>
      </a>
    </li>
    <li><a href="/docs/">Documentation</a></li>
    <li><a href="/docs/patterns/">Patterns</a></li>
    <li><span aria-current="page">Breadcrumb</span></li>
  </ol>
</nav>`;

const collapsedCode = `<nav class="breadcrumb" data-collapsed aria-label="Breadcrumb">
  <ol>
    <li><a href="/">Home</a></li>
    <li><a href="/docs/">Documentation</a></li>
    <li><a href="/docs/elements/">Elements</a></li>
    <li><a href="/docs/elements/nav/">Navigation</a></li>
    <li><a href="/docs/patterns/">Patterns</a></li>
    <li><span aria-current="page">Breadcrumb</span></li>
  </ol>
</nav>`;

const separatorChevronCode = `<nav class="breadcrumb" data-separator="chevron" aria-label="Breadcrumb">
  <ol>
    <li><a href="/">Home</a></li>
    <li><a href="/docs/">Documentation</a></li>
    <li><span aria-current="page">Patterns</span></li>
  </ol>
</nav>`;

const separatorArrowCode = `<nav class="breadcrumb" data-separator="arrow" aria-label="Breadcrumb">
  <ol>
    <li><a href="/">Home</a></li>
    <li><a href="/docs/">Documentation</a></li>
    <li><span aria-current="page">Patterns</span></li>
  </ol>
</nav>`;

const truncatedCode = `<nav class="breadcrumb" aria-label="Breadcrumb">
  <ol>
    <li><a href="/">Home</a></li>
    <li><a href="/products/" data-truncated>Very Long Category Name That Needs Truncation</a></li>
    <li><span aria-current="page">Product</span></li>
  </ol>
</nav>`;

const breadcrumbStylesCode = `/* Breadcrumb base styles (included in VB) */
nav.breadcrumb > ol {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: var(--size-2xs);
  font-size: var(--font-size-s);
}

nav.breadcrumb li {
  display: flex;
  align-items: center;
  gap: var(--size-2xs);
}

/* Separator (CSS-generated) */
nav.breadcrumb li:not(:last-child)::after {
  content: var(--_separator, "/");
  margin-inline-start: var(--size-2xs);
  color: var(--color-gray-400);
}

/* Links and current page */
nav.breadcrumb a,
nav.breadcrumb span {
  padding: var(--size-xs);
  color: var(--color-text-muted);
  text-decoration: none;
}

nav.breadcrumb a:hover {
  color: var(--color-text);
  text-decoration: underline;
}

nav.breadcrumb [aria-current="page"] {
  color: var(--color-text);
  font-weight: var(--font-weight-medium);
}`;

const separatorStylesCode = `/* Separator variants */
nav.breadcrumb[data-separator="chevron"] {
  --_separator: "\\203A"; /* › */
}

nav.breadcrumb[data-separator="arrow"] {
  --_separator: "\\2192"; /* → */
}

nav.breadcrumb[data-separator="dot"] {
  --_separator: "\\00B7"; /* · */
}

nav.breadcrumb[data-separator="pipe"] {
  --_separator: "|";
}`;

const collapsedStylesCode = `/* Collapsed breadcrumb (CSS-only) */
nav.breadcrumb[data-collapsed] li:not(:first-child):not(:last-child):not(:nth-last-child(2)) {
  display: none;
}

nav.breadcrumb[data-collapsed] li:nth-last-child(2)::before {
  content: "...";
  margin-inline-end: var(--size-2xs);
  color: var(--color-gray-400);
}

/* Truncated items */
nav.breadcrumb [data-truncated] {
  max-width: 10rem;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}`;
---

<DocsLayout
  title="Breadcrumb Patterns"
  description="Breadcrumb navigation patterns for showing hierarchical page location with accessible markup."
  currentSection="examples"
>
  <nav class="breadcrumb" aria-label="Breadcrumb">
    <a href="/docs/patterns/">Patterns</a> / <span aria-current="page">Breadcrumb</span>
  </nav>

  <h1>Breadcrumb Patterns</h1>
  <p class="lead">Breadcrumb navigation helps users understand their location within a site hierarchy and provides quick access to parent pages. Built with semantic HTML and accessible by default.</p>

  <section>
    <h2>Overview</h2>
    <layout-text>
      <p>Breadcrumbs use the semantic <code>&lt;nav&gt;</code> element with <code>aria-label="Breadcrumb"</code> and an ordered list (<code>&lt;ol&gt;</code>) to represent the hierarchical structure. The current page is marked with <code>aria-current="page"</code> and rendered as a <code>&lt;span&gt;</code> rather than a link.</p>
      <p><strong>Key features:</strong></p>
      <ul>
        <li>Semantic HTML with <code>&lt;nav&gt;</code> and <code>&lt;ol&gt;</code> for proper structure</li>
        <li>Current page indicated with <code>aria-current="page"</code></li>
        <li>CSS-generated separators (no extra markup needed)</li>
        <li>Multiple separator styles: slash, chevron, arrow, dot, pipe</li>
        <li>Collapsed mode for deep hierarchies</li>
        <li>Text truncation for long labels</li>
      </ul>
    </layout-text>
  </section>

  <section>
    <h2>Simple Breadcrumb</h2>
    <layout-text>
      <p>A basic breadcrumb showing the page hierarchy. Uses the default slash separator. The current page is not a link to avoid self-referential navigation.</p>
    </layout-text>

    <browser-window
      src="/docs/patterns/demos/breadcrumb-simple.html"
      url="https://vanilla-breeze.dev/breadcrumb-simple"
      title="Simple Breadcrumb"
      shadow>
    </browser-window>

    <code-block language="html" label="Simple breadcrumb">{simpleBreadcrumbCode}</code-block>
  </section>

  <section>
    <h2>With Home Icon</h2>
    <layout-text>
      <p>Replace the "Home" text with an icon for a more compact design. Include <code>visually-hidden</code> text for screen reader accessibility.</p>
    </layout-text>

    <browser-window
      src="/docs/patterns/demos/breadcrumb-home-icon.html"
      url="https://vanilla-breeze.dev/breadcrumb-icon"
      title="Breadcrumb with Home Icon"
      shadow>
    </browser-window>

    <code-block language="html" label="Breadcrumb with home icon">{homeIconCode}</code-block>
  </section>

  <section>
    <h2>Collapsed for Deep Hierarchies</h2>
    <layout-text>
      <p>When the breadcrumb trail is long, use <code>data-collapsed</code> to hide middle items. This shows only the first item, an ellipsis indicator, and the last two items.</p>
    </layout-text>

    <browser-window
      src="/docs/patterns/demos/breadcrumb-collapsed.html"
      url="https://vanilla-breeze.dev/breadcrumb-collapsed"
      title="Collapsed Breadcrumb"
      shadow>
    </browser-window>

    <code-block language="html" label="Collapsed breadcrumb">{collapsedCode}</code-block>
  </section>

  <section>
    <h2>Separator Variants</h2>
    <layout-text>
      <p>Use <code>data-separator</code> to change the separator character. Available options: <code>chevron</code>, <code>arrow</code>, <code>dot</code>, <code>pipe</code>.</p>
    </layout-text>

    <h3>Chevron Separator</h3>
    <div class="example">
      <nav class="breadcrumb" data-separator="chevron" aria-label="Breadcrumb">
        <ol>
          <li><a href="#">Home</a></li>
          <li><a href="#">Documentation</a></li>
          <li><span aria-current="page">Patterns</span></li>
        </ol>
      </nav>
    </div>
    <code-block language="html" label="Chevron separator">{separatorChevronCode}</code-block>

    <h3>Arrow Separator</h3>
    <div class="example">
      <nav class="breadcrumb" data-separator="arrow" aria-label="Breadcrumb">
        <ol>
          <li><a href="#">Home</a></li>
          <li><a href="#">Documentation</a></li>
          <li><span aria-current="page">Patterns</span></li>
        </ol>
      </nav>
    </div>
    <code-block language="html" label="Arrow separator">{separatorArrowCode}</code-block>
  </section>

  <section>
    <h2>Truncated Labels</h2>
    <layout-text>
      <p>For breadcrumb items with long labels, use <code>data-truncated</code> to limit width and show ellipsis.</p>
    </layout-text>

    <div class="example">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <ol>
          <li><a href="#">Home</a></li>
          <li><a href="#" data-truncated>Very Long Category Name That Needs Truncation</a></li>
          <li><span aria-current="page">Product</span></li>
        </ol>
      </nav>
    </div>
    <code-block language="html" label="Truncated breadcrumb">{truncatedCode}</code-block>
  </section>

  <section>
    <h2>CSS Reference</h2>
    <layout-text>
      <p>The breadcrumb styles are included in Vanilla Breeze. Here is the CSS for reference:</p>
    </layout-text>

    <h3>Base Styles</h3>
    <code-block language="css" label="Breadcrumb base styles">{breadcrumbStylesCode}</code-block>

    <h3>Separator Variants</h3>
    <code-block language="css" label="Separator variant styles">{separatorStylesCode}</code-block>

    <h3>Collapsed and Truncated</h3>
    <code-block language="css" label="Collapsed and truncated styles">{collapsedStylesCode}</code-block>
  </section>

  <section>
    <h2>Usage Notes</h2>
    <layout-text>
      <ul>
        <li><strong>Landmark:</strong> Always use <code>&lt;nav&gt;</code> with <code>aria-label="Breadcrumb"</code> to identify the navigation purpose</li>
        <li><strong>Ordered list:</strong> Use <code>&lt;ol&gt;</code> to convey hierarchical order to screen readers</li>
        <li><strong>Current page:</strong> Mark with <code>aria-current="page"</code> and use a <code>&lt;span&gt;</code> instead of a link</li>
        <li><strong>Depth limit:</strong> Avoid more than 5 levels; use collapsed mode for deep hierarchies</li>
        <li><strong>Mobile:</strong> Consider horizontal scrolling or collapsing on small screens</li>
        <li><strong>Schema.org:</strong> Add structured data for SEO benefits (see nav element docs)</li>
      </ul>
    </layout-text>
  </section>

  <section>
    <h2>Related</h2>
    <layout-grid data-min="200px" data-gap="m">
      <a href="/docs/elements/native/nav/" class="section-card">
        <h3>Nav Element</h3>
        <p>Full documentation for the nav element including all breadcrumb variants</p>
      </a>
      <a href="/docs/patterns/navigation/navbar/" class="section-card">
        <h3>Navbar</h3>
        <p>Header navigation patterns with dropdowns and mobile menus</p>
      </a>
      <a href="/docs/elements/web-components/icons/" class="section-card">
        <h3>Icons</h3>
        <p>Icon web component for home icon and other breadcrumb icons</p>
      </a>
      <a href="/docs/utilities/visually-hidden/" class="section-card">
        <h3>Visually Hidden</h3>
        <p>Screen reader only text utility</p>
      </a>
    </layout-grid>
  </section>
</DocsLayout>
