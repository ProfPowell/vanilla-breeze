---
/**
 * Quick Start Guide
 *
 * Getting started with Vanilla Breeze - installation, basic setup, and core concepts.
 */
import DocsLayout from '../../layouts/DocsLayout.astro';

// Code examples as template strings to avoid Astro parsing issues
const cdnInstall = `<!-- CSS (required) -->
<link rel="stylesheet" href="https://unpkg.com/vanilla-breeze/dist/cdn/vanilla-breeze.css">

<!-- JavaScript (optional, for interactive components) -->
<script type="module" src="https://unpkg.com/vanilla-breeze/dist/cdn/vanilla-breeze.js"><\/script>`;

const npmInstall = `npm install vanilla-breeze`;

const jsImport = `// Import CSS
import 'vanilla-breeze/css';

// Import JS (optional)
import 'vanilla-breeze';`;

const basicBoilerplate = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Vanilla Breeze Page</title>
  <link rel="stylesheet" href="https://unpkg.com/vanilla-breeze/dist/cdn/vanilla-breeze.css">
</head>
<body>
  <header>
    <h1>Welcome</h1>
  </header>

  <main>
    <layout-stack data-layout-gap="l">
      <p>Your content here.</p>
      <button type="button">Get Started</button>
    </layout-stack>
  </main>

  <footer>
    <p>&copy; 2026 Your Name</p>
  </footer>

  <!-- Optional: Add JS for interactive components -->
  <script type="module" src="https://unpkg.com/vanilla-breeze/dist/cdn/vanilla-breeze.js"><\/script>
</body>
</html>`;

const colorTokens = `.my-card {
  /* Semantic colors adapt to light/dark mode */
  background: var(--color-surface);
  color: var(--color-text);
  border: var(--border-width-thin) solid var(--color-border);
}

.my-button {
  /* Interactive colors for buttons and links */
  background: var(--color-interactive);
}

.my-button:hover {
  background: var(--color-interactive-hover);
}`;

const spacingTokens = `.my-section {
  /* T-shirt sizes: 3xs, 2xs, xs, s, m, l, xl, 2xl, 3xl */
  padding: var(--size-l);
  margin-block-end: var(--size-xl);
  gap: var(--size-m);
}`;

const buttonVariants = `<button type="button">Primary</button>
<button type="button" class="secondary">Secondary</button>
<button type="button" class="ghost">Ghost</button>
<button type="button" disabled>Disabled</button>`;

const layoutStackExample = `<layout-stack data-layout-gap="m">
  <layout-card>Card 1</layout-card>
  <layout-card>Card 2</layout-card>
  <layout-card>Card 3</layout-card>
</layout-stack>`;

const layoutGridExample = `<layout-grid data-layout-min="150px" data-layout-gap="m">
  <layout-card>Card 1</layout-card>
  <layout-card>Card 2</layout-card>
  <layout-card>Card 3</layout-card>
</layout-grid>`;

const layoutClusterExample = `<layout-cluster data-layout-gap="s">
  <button type="button">Save</button>
  <button type="button" class="secondary">Cancel</button>
  <button type="button" class="ghost">Reset</button>
</layout-cluster>`;

const accordionExample = `<accordion-wc>
  <details>
    <summary>What is Vanilla Breeze?</summary>
    <p>A layered HTML component system...</p>
  </details>
  <details>
    <summary>Do I need JavaScript?</summary>
    <p>No! The CSS-only layers work without JavaScript...</p>
  </details>
</accordion-wc>`;

const tabsExample = `<tabs-wc>
  <details open name="quick-start-tabs">
    <summary>HTML</summary>
    <p>HTML content here...</p>
  </details>
  <details name="quick-start-tabs">
    <summary>CSS</summary>
    <p>CSS content here...</p>
  </details>
  <details name="quick-start-tabs">
    <summary>JavaScript</summary>
    <p>JavaScript content here...</p>
  </details>
</tabs-wc>`;
---

<DocsLayout
  title="Quick Start"
  description="Get started with Vanilla Breeze in minutes. Learn installation, basic setup, design tokens, native elements, layout components, and progressive enhancement."
  currentSection="docs"
>
  <h1>Quick Start</h1>
  <p class="lead">Get up and running with Vanilla Breeze in minutes. This guide covers installation, basic setup, and core concepts.</p>

  <nav class="subnav" aria-label="Quick start sections">
    <ul>
      <li><a href="#installation">Installation</a></li>
      <li><a href="#basic-setup">Basic Setup</a></li>
      <li><a href="#design-tokens">Design Tokens</a></li>
      <li><a href="#native-elements">Native Elements</a></li>
      <li><a href="#layout-elements">Layout Elements</a></li>
      <li><a href="#web-components">Web Components</a></li>
      <li><a href="#progressive-enhancement">Progressive Enhancement</a></li>
    </ul>
  </nav>

  <!-- Installation Section -->
  <section id="installation">
    <h2>Installation</h2>
    <p>Choose the installation method that works best for your project.</p>

    <h3>Option 1: CDN (Quickest)</h3>
    <p>Add Vanilla Breeze directly to any HTML file with no build step required.</p>
    <code-block language="html" label="CDN installation">{cdnInstall}</code-block>

    <h3>Option 2: npm</h3>
    <p>Install via npm for build tool integration.</p>
    <code-block language="bash" label="npm installation">{npmInstall}</code-block>

    <p>Then import in your project:</p>
    <code-block language="javascript" label="Import in JavaScript">{jsImport}</code-block>

    <h3>Option 3: Direct Download</h3>
    <p>Download the files and include them locally in your project.</p>
    <ol>
      <li>Download from <a href="https://github.com/profpowell/vanilla-breeze/releases">GitHub Releases</a></li>
      <li>Extract <code>vanilla-breeze.css</code> and <code>vanilla-breeze.js</code> to your project</li>
      <li>Link them in your HTML</li>
    </ol>
  </section>

  <!-- Basic Setup Section -->
  <section id="basic-setup">
    <h2>Basic Page Setup</h2>
    <p>Here is a minimal HTML boilerplate with Vanilla Breeze.</p>

    <code-block language="html" show-lines label="Basic HTML boilerplate">{basicBoilerplate}</code-block>

    <browser-window url="https://example.com">
      <div style="padding: var(--size-l);">
        <header style="margin-block-end: var(--size-l);">
          <h1 style="margin: 0;">Welcome</h1>
        </header>
        <main>
          <layout-stack data-layout-gap="l">
            <p style="margin: 0;">Your content here.</p>
            <button type="button">Get Started</button>
          </layout-stack>
        </main>
        <footer style="margin-block-start: var(--size-xl); color: var(--color-text-muted);">
          <p style="margin: 0;">&copy; 2026 Your Name</p>
        </footer>
      </div>
    </browser-window>
  </section>

  <!-- Design Tokens Section -->
  <section id="design-tokens">
    <h2>Using Design Tokens</h2>
    <p>Vanilla Breeze provides CSS custom properties (design tokens) for consistent styling. These work automatically with light and dark modes.</p>

    <h3>Colors</h3>
    <p>Use semantic color tokens that adapt to the user's theme preference.</p>
    <code-block language="css" label="Color tokens">{colorTokens}</code-block>

    <h3>Spacing</h3>
    <p>Use t-shirt size spacing tokens for consistent layouts.</p>
    <code-block language="css" label="Spacing tokens">{spacingTokens}</code-block>

    <div class="example">
      <layout-stack data-layout-gap="s">
        <p><strong>Spacing scale:</strong></p>
        <layout-cluster data-layout-gap="m">
          <span style="padding: var(--size-xs); background: var(--color-surface-raised); border-radius: var(--radius-s);">xs</span>
          <span style="padding: var(--size-s); background: var(--color-surface-raised); border-radius: var(--radius-s);">s</span>
          <span style="padding: var(--size-m); background: var(--color-surface-raised); border-radius: var(--radius-s);">m</span>
          <span style="padding: var(--size-l); background: var(--color-surface-raised); border-radius: var(--radius-s);">l</span>
          <span style="padding: var(--size-xl); background: var(--color-surface-raised); border-radius: var(--radius-s);">xl</span>
        </layout-cluster>
      </layout-stack>
    </div>

    <p>See the complete <a href="/docs/tokens/">Design Tokens documentation</a> for all available tokens.</p>
  </section>

  <!-- Native Elements Section -->
  <section id="native-elements">
    <h2>Styling Native HTML Elements</h2>
    <p>Vanilla Breeze styles 84+ native HTML elements out of the box. Just use semantic HTML and it looks great.</p>

    <h3>Buttons</h3>
    <p>Style variants are applied with simple class names.</p>
    <div class="example">
      <layout-cluster data-layout-gap="m">
        <button type="button">Primary</button>
        <button type="button" class="secondary">Secondary</button>
        <button type="button" class="ghost">Ghost</button>
        <button type="button" disabled>Disabled</button>
      </layout-cluster>
    </div>
    <code-block language="html" label="Button variants">{buttonVariants}</code-block>

    <h3>Forms and Inputs</h3>
    <p>Form elements are styled consistently and accessibly.</p>
    <div class="example" style="max-inline-size: 25rem;">
      <layout-stack data-layout-gap="m">
        <form-field>
          <label for="qs-name">Name</label>
          <input type="text" id="qs-name" placeholder="Your name" />
        </form-field>
        <form-field>
          <label for="qs-email">Email</label>
          <input type="email" id="qs-email" placeholder="you@example.com" />
        </form-field>
        <button type="submit">Send Message</button>
      </layout-stack>
    </div>

    <h3>Tables</h3>
    <div class="example">
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Role</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Alice Johnson</td>
            <td>Developer</td>
            <td>Active</td>
          </tr>
          <tr>
            <td>Bob Smith</td>
            <td>Designer</td>
            <td>Active</td>
          </tr>
        </tbody>
      </table>
    </div>

    <p>See the full <a href="/docs/elements/">Elements documentation</a> for all styled elements.</p>
  </section>

  <!-- Layout Elements Section -->
  <section id="layout-elements">
    <h2>Using Layout Custom Elements</h2>
    <p>Vanilla Breeze provides CSS-only custom elements for common layout patterns. No JavaScript required.</p>

    <h3>layout-stack</h3>
    <p>Vertical stacking with consistent gaps. The fundamental building block for vertical rhythm.</p>
    <div class="example">
      <layout-stack data-layout-gap="m">
        <layout-card>Card 1</layout-card>
        <layout-card>Card 2</layout-card>
        <layout-card>Card 3</layout-card>
      </layout-stack>
    </div>
    <code-block language="html" label="layout-stack example">{layoutStackExample}</code-block>

    <h3>layout-grid</h3>
    <p>Responsive grid that auto-fits columns based on minimum item width.</p>
    <div class="example">
      <layout-grid data-layout-min="150px" data-layout-gap="m">
        <layout-card><p style="margin: 0; text-align: center;">Card 1</p></layout-card>
        <layout-card><p style="margin: 0; text-align: center;">Card 2</p></layout-card>
        <layout-card><p style="margin: 0; text-align: center;">Card 3</p></layout-card>
      </layout-grid>
    </div>
    <code-block language="html" label="layout-grid example">{layoutGridExample}</code-block>

    <h3>layout-cluster</h3>
    <p>Horizontal grouping with wrapping. Perfect for button groups, tags, and inline items.</p>
    <div class="example">
      <layout-cluster data-layout-gap="s">
        <button type="button">Save</button>
        <button type="button" class="secondary">Cancel</button>
        <button type="button" class="ghost">Reset</button>
      </layout-cluster>
    </div>
    <code-block language="html" label="layout-cluster example">{layoutClusterExample}</code-block>

    <h3>Common Attributes</h3>
    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>data-gap</code></td>
          <td><code>none</code>, <code>xs</code>, <code>s</code>, <code>m</code>, <code>l</code>, <code>xl</code></td>
          <td>Controls spacing between child elements</td>
        </tr>
        <tr>
          <td><code>data-align</code></td>
          <td><code>start</code>, <code>center</code>, <code>end</code>, <code>stretch</code></td>
          <td>Controls alignment of child elements</td>
        </tr>
      </tbody>
    </table>

    <p>See the full <a href="/docs/elements/">Custom Elements documentation</a> for all layout elements.</p>
  </section>

  <!-- Web Components Section -->
  <section id="web-components">
    <h2>Adding Web Components for Interactivity</h2>
    <p>For interactive features, Vanilla Breeze provides JavaScript web components. These require the JS bundle.</p>

    <h3>Accordion</h3>
    <p>Collapsible content sections with proper ARIA support.</p>
    <div class="example">
      <accordion-wc>
        <details>
          <summary>What is Vanilla Breeze?</summary>
          <p>Vanilla Breeze is a layered HTML component system. Build with native HTML, enhance with CSS, add interactivity with JavaScript - each layer optional.</p>
        </details>
        <details>
          <summary>Do I need JavaScript?</summary>
          <p>No! The CSS-only layers work without JavaScript. Add JS only when you need interactive components.</p>
        </details>
      </accordion-wc>
    </div>
    <code-block language="html" label="Accordion example">{accordionExample}</code-block>

    <h3>Tabs</h3>
    <p>Tabbed interface for organizing content.</p>
    <div class="example">
      <tabs-wc>
        <details open name="qs-tabs">
          <summary>HTML</summary>
          <p>Write semantic HTML5 markup. Vanilla Breeze styles it automatically.</p>
        </details>
        <details name="qs-tabs">
          <summary>CSS</summary>
          <p>Use CSS custom properties (design tokens) for consistent styling.</p>
        </details>
        <details name="qs-tabs">
          <summary>JavaScript</summary>
          <p>Add JavaScript only when you need interactive components.</p>
        </details>
      </tabs-wc>
    </div>
    <code-block language="html" label="Tabs example">{tabsExample}</code-block>

    <h3>Icons</h3>
    <p>Access 200+ Lucide icons with the icon-wc component.</p>
    <div class="example">
      <layout-cluster data-layout-gap="m">
        <icon-wc name="home"></icon-wc>
        <icon-wc name="search"></icon-wc>
        <icon-wc name="settings"></icon-wc>
        <icon-wc name="user"></icon-wc>
        <icon-wc name="heart"></icon-wc>
        <icon-wc name="star"></icon-wc>
      </layout-cluster>
    </div>
  </section>

  <!-- Progressive Enhancement Section -->
  <section id="progressive-enhancement">
    <h2>Progressive Enhancement Philosophy</h2>
    <p>Vanilla Breeze is built on progressive enhancement. Each layer adds capabilities without breaking the previous layer.</p>

    <h3>The Three Layers</h3>
    <layout-grid data-layout-min="250px" data-layout-gap="m">
      <layout-card>
        <layout-stack data-layout-gap="s">
          <h4 style="margin: 0; display: flex; align-items: center; gap: var(--size-xs);">
            <icon-wc name="file-code" size="sm"></icon-wc>
            Layer 1: HTML
          </h4>
          <p style="margin: 0; color: var(--color-text-muted); font-size: var(--font-size-sm);">Start with semantic HTML5. Content is accessible and functional with no styling.</p>
        </layout-stack>
      </layout-card>
      <layout-card>
        <layout-stack data-layout-gap="s">
          <h4 style="margin: 0; display: flex; align-items: center; gap: var(--size-xs);">
            <icon-wc name="palette" size="sm"></icon-wc>
            Layer 2: CSS
          </h4>
          <p style="margin: 0; color: var(--color-text-muted); font-size: var(--font-size-sm);">Add styling with design tokens and CSS custom elements. Works without JavaScript.</p>
        </layout-stack>
      </layout-card>
      <layout-card>
        <layout-stack data-layout-gap="s">
          <h4 style="margin: 0; display: flex; align-items: center; gap: var(--size-xs);">
            <icon-wc name="zap" size="sm"></icon-wc>
            Layer 3: JavaScript
          </h4>
          <p style="margin: 0; color: var(--color-text-muted); font-size: var(--font-size-sm);">Enhance with web components for interactivity. Gracefully degrades if JS fails.</p>
        </layout-stack>
      </layout-card>
    </layout-grid>

    <h3>Why This Matters</h3>
    <ul>
      <li><strong>Resilience:</strong> If CSS or JS fails to load, content remains accessible</li>
      <li><strong>Performance:</strong> Critical content renders without waiting for JS</li>
      <li><strong>SEO:</strong> Search engines can index all content</li>
      <li><strong>Accessibility:</strong> Screen readers and assistive tech work on all layers</li>
    </ul>
  </section>

  <!-- Next Steps Section -->
  <section>
    <h2>Next Steps</h2>
    <p>Now that you understand the basics, explore more:</p>

    <layout-grid data-layout-min="200px" data-layout-gap="m">
      <a href="/docs/tokens/" class="section-card">
        <h3>Design Tokens</h3>
        <p>Complete reference for colors, spacing, typography, and more.</p>
      </a>
      <a href="/docs/elements/" class="section-card">
        <h3>Elements</h3>
        <p>See all 84+ styled HTML elements.</p>
      </a>
      <a href="/docs/tutorial/" class="section-card">
        <h3>Tutorial</h3>
        <p>Build a complete landing page step by step.</p>
      </a>
      <a href="/docs/examples/" class="section-card">
        <h3>Examples</h3>
        <p>Full page examples and patterns.</p>
      </a>
    </layout-grid>
  </section>
</DocsLayout>
