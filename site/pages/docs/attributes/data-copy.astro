---
/**
 * data-copy Attribute Documentation
 *
 * Clipboard copy enhancement via data attributes.
 */
import DocsLayout from '../../../layouts/DocsLayout.astro';

const basicValueCode = `<button data-copy="npm install vanilla-breeze">Copy install command</button>`;

const basicTargetCode = `<pre><code id="code-block">const greeting = "Hello, world!";</code></pre>

<button data-copy-target="#code-block">Copy code</button>`;

const withIconCode = `<button data-copy="https://vanilla-breeze.dev">
  <icon-wc name="copy" size="sm"></icon-wc>
  Copy link
</button>`;

const eventCode = `const btn = document.querySelector('[data-copy]');

btn.addEventListener('copy', (e) => \{
  console.log('Copied:', e.detail.text);
\});`;

const codeBlockCode = `<div style="position: relative;">
  <pre><code id="snippet">npm install vanilla-breeze</code></pre>
  <button data-copy-target="#snippet" class="ghost small"
    style="position: absolute; top: 0.5rem; right: 0.5rem;">
    <icon-wc name="copy" size="sm"></icon-wc>
  </button>
</div>`;

const shareCode = `<button data-copy="https://vanilla-breeze.dev/docs" class="secondary">
  <icon-wc name="link" size="sm"></icon-wc>
  Share link
</button>`;

const stylingCode = `[data-copy][data-state="copied"],
[data-copy-target][data-state="copied"] \{
  color: var(--color-success);
\}`;

const dynamicCode = `// Dynamically added buttons are auto-enhanced via MutationObserver
const btn = document.createElement('button');
btn.dataset.copy = 'Hello from JS';
btn.textContent = 'Copy greeting';
document.body.appendChild(btn);
// btn is ready to use — no manual init needed`;
---

<DocsLayout
  title="data-copy"
  description="Copy to clipboard via data attributes. Add data-copy or data-copy-target to any button for clipboard functionality with visual and screen reader feedback."
  currentSection="api"
>
  <h1>data-copy</h1>
  <p class="lead">Copy to clipboard via data attributes. Add <code>data-copy</code> or <code>data-copy-target</code> to any button for clipboard functionality with visual and screen reader feedback.</p>

  <heading-links data-levels="h2,h3">
  <section>
    <h2>Overview</h2>
    <p>The <code>data-copy</code> attribute enhances native buttons with clipboard copy behavior. No wrapper element needed &mdash; just add the attribute directly to a <code>&lt;button&gt;</code>.</p>

    <browser-window
      src="/docs/examples/demos/copy-wc-basic.html"
      url="https://vanilla-breeze.dev/copy"
      title="Copy Button"
      shadow>
    </browser-window>

    <code-block language="html" show-lines label="Copy static text">{basicValueCode}</code-block>
  </section>

  <section>
    <h2>How It Works</h2>
    <p>Add one of these attributes to any <code>&lt;button&gt;</code>:</p>
    <ul>
      <li><code>data-copy</code> &mdash; the attribute value is the text to copy</li>
      <li><code>data-copy-target</code> &mdash; a CSS selector pointing to another element whose <code>textContent</code> will be copied</li>
    </ul>
    <p>On click, the text is written to the clipboard, <code>data-state="copied"</code> is set for 1.5 seconds, a screen reader announcement fires, and a <code>copy</code> custom event is dispatched.</p>
  </section>

  <section>
    <h2>Attributes</h2>
    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Type</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>data-copy</code></td>
          <td>string</td>
          <td>Static text to copy to the clipboard.</td>
        </tr>
        <tr>
          <td><code>data-copy-target</code></td>
          <td>string</td>
          <td>CSS selector for the element whose <code>textContent</code> to copy.</td>
        </tr>
        <tr>
          <td><code>data-state</code></td>
          <td>string</td>
          <td>Set to <code>"copied"</code> automatically for 1.5s after a successful copy. Use in CSS for feedback styling.</td>
        </tr>
        <tr>
          <td><code>data-copy-init</code></td>
          <td>boolean</td>
          <td>Set automatically to prevent double-binding. Do not set manually.</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Copy from Target Element</h2>
    <p>Use <code>data-copy-target</code> with a CSS selector to copy text from another element on the page.</p>

    <div class="example">
      <layout-stack data-layout-gap="m">
        <pre><code id="code-example">const greeting = "Hello, world!";</code></pre>
        <button type="button" data-copy-target="#code-example" class="secondary">Copy code</button>
      </layout-stack>
    </div>

    <code-block language="html" show-lines label="Copy from a target element">{basicTargetCode}</code-block>
  </section>

  <section>
    <h2>With Icons</h2>
    <p>Pair with <code>&lt;icon-wc&gt;</code> for a more visual button.</p>

    <div class="example">
      <button type="button" data-copy="https://vanilla-breeze.dev">
        <icon-wc name="copy" size="sm"></icon-wc>
        Copy link
      </button>
    </div>

    <code-block language="html" show-lines label="Copy button with icon">{withIconCode}</code-block>
  </section>

  <section>
    <h2>Events</h2>
    <p>The button dispatches a <code>copy</code> event on successful clipboard write.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Event</th>
          <th>Detail</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>copy</code></td>
          <td><code>{"{\u200B"} text: string {"}\u200B"}</code></td>
          <td>Fired after text is copied. The <code>detail.text</code> property contains the copied string.</td>
        </tr>
      </tbody>
    </table>

    <code-block language="javascript" show-lines label="Listening for copy events">{eventCode}</code-block>
  </section>

  <section>
    <h2>Common Use Cases</h2>

    <h3>Code Block Copy Button</h3>
    <p>Position a copy button inside a code block:</p>

    <div class="example">
      <div style="position: relative;">
        <pre><code id="snippet-demo">npm install vanilla-breeze</code></pre>
        <button type="button" data-copy-target="#snippet-demo" class="ghost small" style="position: absolute; top: 0.5rem; right: 0.5rem;">
          <icon-wc name="copy" size="sm"></icon-wc>
        </button>
      </div>
    </div>

    <code-block language="html" show-lines label="Code block with copy button">{codeBlockCode}</code-block>

    <h3>Share Link</h3>
    <p>Quick share-by-link button:</p>

    <div class="example">
      <button type="button" data-copy="https://vanilla-breeze.dev/docs" class="secondary">
        <icon-wc name="link" size="sm"></icon-wc>
        Share link
      </button>
    </div>

    <code-block language="html" show-lines label="Share link button">{shareCode}</code-block>
  </section>

  <section>
    <h2>Styling Feedback</h2>
    <p>The <code>data-state="copied"</code> attribute is set for 1.5 seconds after copying. Use it to style the copied state:</p>

    <code-block language="css" show-lines label="Custom copied state styling">{stylingCode}</code-block>

    <p>The default styles change the button text color to <code>--color-success</code> during the copied state.</p>
  </section>

  <section>
    <h2>Dynamic Buttons</h2>
    <p>Buttons added to the DOM after page load are automatically enhanced via a MutationObserver. No manual initialization is needed.</p>

    <code-block language="javascript" show-lines label="Dynamically added copy buttons">{dynamicCode}</code-block>
  </section>

  <section>
    <h2>Accessibility</h2>
    <ul>
      <li>Uses <code>aria-live="polite"</code> to announce "Copied to clipboard" to screen readers</li>
      <li>The announcement element is visually hidden with <code>sr-only</code> and removed after 1 second</li>
      <li>Works with any focusable button element, preserving keyboard accessibility</li>
      <li>No wrapper element &mdash; the button <em>is</em> the interactive element, improving the accessibility tree</li>
      <li>Falls gracefully when the Clipboard API is unavailable or permission is denied</li>
    </ul>
  </section>

  </heading-links>
</DocsLayout>
