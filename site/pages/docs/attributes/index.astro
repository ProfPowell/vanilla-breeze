---
/**
 * Layout Attributes Documentation
 *
 * Complete reference for data-layout and related attributes.
 * Alternative to custom element wrappers with better HTML semantics.
 */
import DocsLayout from '../../../layouts/DocsLayout.astro';

// Code examples
const stackExample = `<article data-layout="stack" data-layout-gap="m">
  <h2>Article Title</h2>
  <p>First paragraph...</p>
  <p>Second paragraph...</p>
</article>`;

const clusterExample = `<nav data-layout-justify="center">
  <ul data-layout="cluster" data-layout-gap="s">
    <li><a href="/">Home</a></li>
    <li><a href="/about">About</a></li>
    <li><a href="/contact">Contact</a></li>
  </ul>
</nav>`;

const gridExample = `<section data-layout="grid" data-layout-min="15rem" data-layout-gap="m">
  <article>Card 1</article>
  <article>Card 2</article>
  <article>Card 3</article>
</section>`;

const centerExample = `<main data-layout="center" data-layout-max="prose">
  <h1>Centered Content</h1>
  <p>This content is centered with a comfortable reading width...</p>
</main>`;

const sidebarExample = `<div data-layout="sidebar" data-layout-sidebar-width="wide">
  <nav>Sidebar navigation...</nav>
  <main>Main content area...</main>
</div>`;

const pageLayoutExample = `<body data-page-layout="sidebar-left">
  <header>Site header...</header>
  <nav>Sidebar navigation...</nav>
  <main>Page content...</main>
  <footer>Site footer...</footer>
</body>`;

const dashboardExample = `<body data-page-layout="dashboard">
  <header data-layout-sticky>Dashboard header...</header>
  <nav data-layout-sticky>Navigation sidebar...</nav>
  <main data-layout="stack" data-layout-gap="l">
    Content area with nested stack layout...
  </main>
</body>`;

const coverExample = `<section data-layout="cover" data-layout-min="100vh" data-layout-centered>
  <header>Top content</header>
  <div data-layout-principal>
    <h1>Hero headline centered vertically</h1>
  </div>
  <footer>Bottom content</footer>
</section>`;

const compositionBeforeExample = `<layout-center data-layout-max="wide">
  <layout-stack data-layout-gap="xl">
    <h2>Title</h2>
    <layout-grid data-layout-min="250px">
      <div>Card 1</div>
      <div>Card 2</div>
      <div>Card 3</div>
    </layout-grid>
  </layout-stack>
</layout-center>`;

const compositionAfterExample = `<section data-layout="center" data-layout-max="wide" data-layout-gap="xl">
  <h2>Title</h2>
  <div data-layout="grid" data-layout-min="250px">
    <div>Card 1</div>
    <div>Card 2</div>
    <div>Card 3</div>
  </div>
</section>`;
---

<DocsLayout
  title="Layout Attributes"
  description="Data attributes for semantic layouts. Use data-layout on any HTML element instead of custom element wrappers."
  currentSection="api"
>
  <h1>Layout Attributes</h1>
  <p class="lead">Apply powerful layouts to semantic HTML elements using data attributes. An alternative to custom elements with better accessibility and HTML validation.</p>

  <nav class="subnav" aria-label="Attribute categories">
    <ul>
      <li><a href="#core-layouts">Core Layouts</a></li>
      <li><a href="#page-layouts">Page Layouts</a></li>
      <li><a href="#special-layouts">Special Layouts</a></li>
      <li><a href="#modifiers">Modifiers</a></li>
      <li><a href="#utilities">Utilities</a></li>
      <li><a href="#layout-composition">Layout Composition</a></li>
    </ul>
  </nav>

  <!-- Core Layouts Section -->
  <section id="core-layouts">
    <h2>Core Layouts</h2>
    <p>Fundamental layout patterns for everyday use.</p>

    <h3 id="stack">Stack</h3>
    <p>Vertical layout with consistent gaps. The foundation of vertical rhythm.</p>

    <div class="example">
      <article data-layout="stack" data-layout-gap="m" style="max-width: 20rem; padding: var(--size-m); background: var(--color-surface-raised); border-radius: var(--radius-m);">
        <h4 style="margin: 0;">Article Title</h4>
        <p style="margin: 0;">First paragraph of content goes here.</p>
        <p style="margin: 0;">Second paragraph continues the story.</p>
      </article>
    </div>

    <code-block language="html" label="Stack layout">{stackExample}</code-block>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-gap</code></td><td><code>none</code>, <code>3xs</code>-<code>3xl</code></td><td>Spacing between items</td></tr>
        <tr><td><code>data-layout-align</code></td><td><code>start</code>, <code>center</code>, <code>end</code>, <code>stretch</code></td><td>Cross-axis alignment</td></tr>
      </tbody>
    </table>

    <h3 id="cluster">Cluster</h3>
    <p>Horizontal layout with wrapping. Perfect for navigation, tags, and button groups.</p>

    <div class="example">
      <nav>
        <ul data-layout="cluster" data-layout-gap="s">
          <li><a href="#">Home</a></li>
          <li><a href="#">About</a></li>
          <li><a href="#">Services</a></li>
          <li><a href="#">Contact</a></li>
        </ul>
      </nav>
    </div>

    <code-block language="html" label="Cluster layout">{clusterExample}</code-block>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-gap</code></td><td><code>xs</code>-<code>xl</code></td><td>Spacing between items</td></tr>
        <tr><td><code>data-layout-justify</code></td><td><code>start</code>, <code>end</code>, <code>center</code>, <code>between</code></td><td>Main axis distribution</td></tr>
        <tr><td><code>data-layout-align</code></td><td><code>start</code>, <code>end</code>, <code>center</code>, <code>stretch</code>, <code>baseline</code></td><td>Cross-axis alignment</td></tr>
        <tr><td><code>data-layout-nowrap</code></td><td>boolean</td><td>Prevent wrapping</td></tr>
        <tr><td><code>data-layout-overlap</code></td><td><code>xs</code>-<code>l</code></td><td>Overlap items (avatar stacks)</td></tr>
      </tbody>
    </table>

    <h3 id="grid">Grid</h3>
    <p>Responsive auto-fit grid. Items automatically wrap based on minimum width.</p>

    <div class="example">
      <section data-layout="grid" data-layout-min="10rem" data-layout-gap="m">
        <div style="padding: var(--size-m); background: var(--color-surface-raised); border-radius: var(--radius-m); text-align: center;">Card 1</div>
        <div style="padding: var(--size-m); background: var(--color-surface-raised); border-radius: var(--radius-m); text-align: center;">Card 2</div>
        <div style="padding: var(--size-m); background: var(--color-surface-raised); border-radius: var(--radius-m); text-align: center;">Card 3</div>
        <div style="padding: var(--size-m); background: var(--color-surface-raised); border-radius: var(--radius-m); text-align: center;">Card 4</div>
      </section>
    </div>

    <code-block language="html" label="Grid layout">{gridExample}</code-block>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-min</code></td><td><code>6rem</code>-<code>25rem</code>, <code>150px</code>, <code>200px</code>, <code>250px</code>, <code>280px</code>, <code>300px</code>, <code>400px</code></td><td>Minimum item width (rem or px)</td></tr>
        <tr><td><code>data-layout-gap</code></td><td><code>none</code>, <code>xs</code>-<code>xl</code></td><td>Spacing between items</td></tr>
      </tbody>
    </table>

    <h3 id="center">Center</h3>
    <p>Horizontally centered container with max-width constraint.</p>

    <div class="example">
      <div style="background: var(--color-surface-sunken); padding: var(--size-m);">
        <main data-layout="center" data-layout-max="narrow" style="background: var(--color-surface); padding: var(--size-m); border-radius: var(--radius-m);">
          <p style="margin: 0;">Centered content with controlled width. Perfect for article layouts and readable text.</p>
        </main>
      </div>
    </div>

    <code-block language="html" label="Center layout">{centerExample}</code-block>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-max</code></td><td><code>narrow</code>, <code>normal</code>, <code>wide</code>, <code>prose</code></td><td>Maximum width</td></tr>
        <tr><td><code>data-layout-gutter</code></td><td><code>none</code>, <code>s</code>, <code>l</code></td><td>Horizontal padding</td></tr>
        <tr><td><code>data-layout-gap</code></td><td><code>none</code>, <code>3xs</code>-<code>3xl</code></td><td>Vertical spacing between children (switches to flex column)</td></tr>
        <tr><td><code>data-layout-intrinsic</code></td><td>boolean</td><td>Center based on content width</td></tr>
        <tr><td><code>data-layout-text</code></td><td>boolean</td><td>Also center text alignment</td></tr>
      </tbody>
    </table>

    <layout-text>
      <p>When <code>data-layout-gap</code> is present on a center element, it switches from <code>display: block</code> to <code>display: flex; flex-direction: column</code>. This enables vertical spacing between children without needing a nested stack. Accepted values: <code>none</code>, <code>3xs</code>, <code>2xs</code>, <code>xs</code>, <code>s</code>, <code>m</code>, <code>l</code>, <code>xl</code>, <code>2xl</code>, <code>3xl</code>.</p>
    </layout-text>

    <h3 id="sidebar">Sidebar</h3>
    <p>Two-column layout with flexible sidebar. Semantic-aware: nav/aside become sidebar, main/article become content.</p>

    <div class="example">
      <div data-layout="sidebar" data-layout-sidebar-width="narrow" data-layout-gap="m" style="min-height: 8rem;">
        <nav style="background: var(--color-surface-raised); padding: var(--size-m); border-radius: var(--radius-m);">
          <p style="margin: 0;">Sidebar</p>
        </nav>
        <main style="background: var(--color-surface-raised); padding: var(--size-m); border-radius: var(--radius-m);">
          <p style="margin: 0;">Main content area</p>
        </main>
      </div>
    </div>

    <code-block language="html" label="Sidebar layout">{sidebarExample}</code-block>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-side</code></td><td><code>start</code> (default), <code>end</code></td><td>Sidebar position</td></tr>
        <tr><td><code>data-layout-sidebar-width</code></td><td><code>narrow</code>, <code>normal</code>, <code>wide</code></td><td>Sidebar width</td></tr>
        <tr><td><code>data-layout-content-min</code></td><td><code>40</code>, <code>50</code>, <code>60</code></td><td>Content min-width %</td></tr>
        <tr><td><code>data-layout-gap</code></td><td><code>xs</code>-<code>xl</code></td><td>Gap between sidebar and content</td></tr>
        <tr><td><code>data-layout-nowrap</code></td><td>boolean</td><td>Prevent stacking on mobile</td></tr>
      </tbody>
    </table>

    <h3 id="split">Split</h3>
    <p>Two-column ratio-based layout. Great for hero sections and content with media.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-ratio</code></td><td><code>1:2</code>, <code>2:1</code>, <code>1:3</code>, <code>3:1</code>, <code>golden</code></td><td>Column ratio</td></tr>
        <tr><td><code>data-layout-gap</code></td><td><code>s</code>-<code>xl</code></td><td>Gap between columns</td></tr>
        <tr><td><code>data-layout-align</code></td><td><code>start</code>, <code>center</code>, <code>end</code>, <code>stretch</code></td><td>Vertical alignment</td></tr>
        <tr><td><code>data-layout-fill</code></td><td>boolean</td><td>Full viewport height (<code>100dvh</code>), ideal for sign-in pages</td></tr>
      </tbody>
    </table>
  </section>

  <!-- Page Layouts Section -->
  <section id="page-layouts">
    <h2>Page Layouts</h2>
    <p>Full-page layouts using <code>data-page-layout</code> on the <code>&lt;body&gt;</code> element. Semantic children (header, nav, main, aside, footer) are auto-assigned to grid areas.</p>

    <h3>Available Page Layouts</h3>
    <table class="props-table">
      <thead>
        <tr>
          <th>Value</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>stack</code></td><td>Single column (mobile-first default)</td></tr>
        <tr><td><code>sidebar-left</code></td><td>Two-column with left navigation</td></tr>
        <tr><td><code>sidebar-right</code></td><td>Two-column with right sidebar</td></tr>
        <tr><td><code>holy-grail</code></td><td>Three-column classic layout</td></tr>
        <tr><td><code>app-shell</code></td><td>Vertical nav bar for apps</td></tr>
        <tr><td><code>dashboard</code></td><td>Admin panel with sticky nav</td></tr>
        <tr><td><code>article</code></td><td>Centered content for reading</td></tr>
        <tr><td><code>landing</code></td><td>Marketing page with named sections</td></tr>
      </tbody>
    </table>

    <h3>Example: Dashboard Layout</h3>
    <code-block language="html" label="Dashboard page layout">{dashboardExample}</code-block>

    <h3>Dashboard Modifiers</h3>
    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-header-height</code></td><td><code>3rem</code>, <code>3.5rem</code>, <code>4rem</code></td><td>Override dashboard header height</td></tr>
        <tr><td><code>data-nav-open</code></td><td>boolean</td><td>Show mobile nav overlay (toggle on small viewports)</td></tr>
      </tbody>
    </table>

    <h3>Semantic Grid Identity</h3>
    <p>Semantic elements automatically register to grid areas:</p>
    <table class="props-table">
      <thead>
        <tr>
          <th>Element</th>
          <th>Grid Area</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>&lt;header&gt;</code></td><td><code>body-header</code></td></tr>
        <tr><td><code>&lt;nav&gt;</code></td><td><code>body-nav</code></td></tr>
        <tr><td><code>&lt;main&gt;</code></td><td><code>body-main</code></td></tr>
        <tr><td><code>&lt;aside&gt;</code></td><td><code>body-aside</code></td></tr>
        <tr><td><code>&lt;footer&gt;</code></td><td><code>body-footer</code></td></tr>
      </tbody>
    </table>

    <h3>Content-Aware Adaptation</h3>
    <p>Layouts automatically adapt when elements are missing:</p>
    <ul>
      <li><code>holy-grail</code> without <code>&lt;aside&gt;</code> becomes two-column</li>
      <li><code>sidebar-left</code> without <code>&lt;nav&gt;</code> becomes single-column</li>
      <li>All layouts collapse to stack on mobile (unless <code>data-layout-nowrap</code> is set)</li>
    </ul>

    <h3>Basic Page Layout</h3>
    <code-block language="html" label="Page layout with sidebar">{pageLayoutExample}</code-block>
  </section>

  <!-- Special Layouts Section -->
  <section id="special-layouts">
    <h2>Special Layouts</h2>

    <h3 id="cover">Cover</h3>
    <p>Full-height container with vertically centered principal element. Perfect for hero sections.</p>

    <code-block language="html" label="Cover layout">{coverExample}</code-block>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-min</code></td><td><code>50vh</code>, <code>75vh</code>, <code>100vh</code>, <code>100dvh</code>, <code>auto</code></td><td>Minimum height</td></tr>
        <tr><td><code>data-layout-padding</code></td><td><code>none</code>, <code>s</code>-<code>xl</code></td><td>Internal padding</td></tr>
        <tr><td><code>data-layout-centered</code></td><td>boolean</td><td>Center align contents</td></tr>
        <tr><td><code>data-layout-principal</code></td><td>child attribute</td><td>Mark the centered element</td></tr>
      </tbody>
    </table>

    <h3 id="switcher">Switcher</h3>
    <p>Flexbox layout that switches from horizontal to vertical at a threshold width.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-threshold</code></td><td><code>20rem</code>-<code>45rem</code></td><td>Switch breakpoint</td></tr>
        <tr><td><code>data-layout-gap</code></td><td><code>none</code>, <code>xs</code>-<code>xl</code></td><td>Spacing between items</td></tr>
        <tr><td><code>data-layout-limit</code></td><td><code>2</code>, <code>3</code>, <code>4</code></td><td>Max items before forcing vertical</td></tr>
        <tr><td><code>data-layout-reverse</code></td><td>boolean</td><td>Reverse item order</td></tr>
      </tbody>
    </table>

    <h3 id="prose">Prose</h3>
    <p>Typography utility for readable text content with controlled line length.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-max</code></td><td><code>narrow</code> (45ch), <code>normal</code> (65ch), <code>wide</code> (80ch)</td><td>Maximum line length</td></tr>
        <tr><td><code>data-layout-centered</code></td><td>boolean</td><td>Center the prose block</td></tr>
      </tbody>
    </table>

    <h3 id="regions">Regions</h3>
    <p>Generalized header / content / footer grid. Semantic children auto-place without explicit attributes. <a href="/docs/grid-identity/#component-level">Full documentation &rarr;</a></p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-gap</code></td><td><code>none</code>, <code>3xs</code>-<code>3xl</code></td><td>Spacing between regions</td></tr>
      </tbody>
    </table>

    <h3 id="media">Media</h3>
    <p>Figure + content side by side (the media object pattern). <a href="/docs/grid-identity/#component-level">Full documentation &rarr;</a></p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Values</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>data-layout-gap</code></td><td><code>none</code>, <code>xs</code>-<code>xl</code></td><td>Gap between figure and content</td></tr>
        <tr><td><code>data-layout-reverse</code></td><td>boolean</td><td>Content on left, figure on right</td></tr>
        <tr><td><code>data-layout-align</code></td><td><code>start</code>, <code>center</code>, <code>end</code>, <code>stretch</code></td><td>Vertical alignment</td></tr>
      </tbody>
    </table>
  </section>

  <!-- Modifiers Section -->
  <section id="modifiers">
    <h2>Gap Modifier</h2>
    <p>The <code>data-layout-gap</code> attribute controls spacing in all layout types.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Value</th>
          <th>Size Token</th>
          <th>Pixels</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>none</code></td><td>0</td><td>0px</td></tr>
        <tr><td><code>3xs</code></td><td><code>--size-3xs</code></td><td>2px</td></tr>
        <tr><td><code>2xs</code></td><td><code>--size-2xs</code></td><td>4px</td></tr>
        <tr><td><code>xs</code></td><td><code>--size-xs</code></td><td>8px</td></tr>
        <tr><td><code>s</code></td><td><code>--size-s</code></td><td>12px</td></tr>
        <tr><td><code>m</code></td><td><code>--size-m</code></td><td>16px</td></tr>
        <tr><td><code>l</code></td><td><code>--size-l</code></td><td>24px</td></tr>
        <tr><td><code>xl</code></td><td><code>--size-xl</code></td><td>32px</td></tr>
        <tr><td><code>2xl</code></td><td><code>--size-2xl</code></td><td>48px</td></tr>
        <tr><td><code>3xl</code></td><td><code>--size-3xl</code></td><td>64px</td></tr>
      </tbody>
    </table>
  </section>

  <!-- Utilities Section -->
  <section id="utilities">
    <h2>Utilities</h2>
    <p>Helper attributes that work with page layouts.</p>

    <h3 id="view-transitions">View Transitions</h3>
    <p>CSS View Transitions enable smooth animated navigation between pages. Named groups, shared element morphing, animation presets, and an init script for declarative naming.</p>
    <p><a href="/docs/attributes/view-transitions/">Full View Transitions documentation &rarr;</a></p>

    <h3>Layout Utilities</h3>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code><a href="/docs/attributes/data-copy/">data-copy</a></code></td><td>Copy text to clipboard on click (<a href="/docs/attributes/data-copy/">full docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-hotkey/">data-hotkey</a></code></td><td>Platform-aware keyboard shortcut display (<a href="/docs/attributes/data-hotkey/">full docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-format-number/">data-format-number</a></code></td><td>Locale-aware number formatting (<a href="/docs/attributes/data-format-number/">full docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-format-date/">data-format-date</a></code></td><td>Locale-aware date and relative time formatting (<a href="/docs/attributes/data-format-date/">full docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-format-bytes/">data-format-bytes</a></code></td><td>Human-readable file size formatting (<a href="/docs/attributes/data-format-bytes/">full docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-ticker/">data-ticker</a></code></td><td>Animated number count-up on scroll (<a href="/docs/attributes/data-ticker/">full docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-spoiler/">data-spoiler</a></code></td><td>Content concealment with blur, solid, or noise effects (<a href="/docs/attributes/data-spoiler/">full docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/web-components/splitter/">data-splitter</a></code></td><td>Inject a resizable drag divider between panels (<a href="/docs/elements/web-components/splitter/">full docs</a>)</td></tr>
        <tr><td><code>data-layout-sticky</code></td><td>Make header, nav, or aside sticky</td></tr>
        <tr><td><code>data-layout-bleed</code></td><td>Span all grid columns (full-width)</td></tr>
        <tr><td><code>data-layout-nowrap</code></td><td>Prevent responsive stacking</td></tr>
        <tr><td><code>data-layout-sidebar="collapsed"</code></td><td>Use collapsed sidebar width</td></tr>
        <tr><td><code>data-layout-sidebar="hidden"</code></td><td>Hide the sidebar nav</td></tr>
        <tr><td><code>data-layout-order="-1|1|99"</code></td><td>Reorder elements</td></tr>
        <tr><td><code>data-layout-area</code></td><td>Explicit grid area assignment</td></tr>
        <tr><td><code>data-container</code></td><td>Establish inline-size containment. Presets: <code>card</code>, <code>panel</code>, <code>media</code> (<a href="/docs/grid-identity/#quick-reference">details</a>)</td></tr>
      </tbody>
    </table>

    <h3>Form Utilities</h3>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code><a href="/docs/elements/native/input/#toggle-switch-data-switch">data-switch</a></code></td><td>Checkbox styled as on/off toggle switch (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/textarea/#character--word-count-data-count">data-count</a></code></td><td>Live character or word counter for textareas (<a href="/docs/elements/native/textarea/">textarea docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/textarea/#auto-expand-data-grow">data-grow</a></code></td><td>Auto-expanding textarea that grows to fit content (<a href="/docs/elements/native/textarea/">textarea docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#password-strength-data-strength">data-strength</a></code></td><td>Real-time password strength meter and rules checklist (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/form/#conditional-fields-data-show-when">data-show-when</a></code></td><td>Show form section when field equals a value (<a href="/docs/elements/native/form/">form docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/form/#conditional-fields-data-show-when">data-hide-when</a></code></td><td>Hide form section when field equals a value (<a href="/docs/elements/native/form/">form docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#range-enhancement-data-range">data-range</a></code></td><td>Cross-browser styled range with track fill (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#range-enhancement-data-range">data-bubble</a></code></td><td>Floating value bubble for range inputs (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#file-upload-zone-data-upload">data-upload</a></code></td><td>Drag-and-drop file upload zone (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/form/#form-autosave-data-autosave">data-autosave</a></code></td><td>Persist form drafts to localStorage (<a href="/docs/elements/native/form/">form docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#input-masking-data-mask">data-mask</a></code></td><td>Format text inputs as user types &mdash; phone, credit card, date, SSN, zip, custom (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#range-markers-data-markers">data-markers</a></code></td><td>Tick marks at each step on range sliders (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#toggle-tags-data-toggle-tags">data-toggle-tags</a></code></td><td>Checkbox pill chips for multi-select &mdash; CSS-only with optional max limit (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#number-stepper-data-stepper">data-stepper</a></code></td><td>Custom +/&minus; buttons on number inputs (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/elements/native/input/#enhanced-color-input-data-color">data-color</a></code></td><td>Swatch circle and hex display for color inputs (<a href="/docs/elements/native/input/">input docs</a>)</td></tr>
        <tr><td><code><a href="/docs/patterns/forms/wizard/">data-wizard</a></code></td><td>Multi-step wizard controller &mdash; auto-enhances forms with step navigation, validation, and progress (<a href="/docs/patterns/forms/wizard/">wizard docs</a>)</td></tr>
        <tr><td><code><a href="/docs/patterns/forms/wizard/#markup-reference">data-wizard-step</a></code></td><td>Mark a <code>&lt;fieldset&gt;</code> as a wizard step (<a href="/docs/patterns/forms/wizard/">wizard docs</a>)</td></tr>
        <tr><td><code><a href="/docs/patterns/forms/wizard/#markup-reference">data-wizard-if</a></code></td><td>Conditional step &mdash; show only when a form field matches a value (<a href="/docs/patterns/forms/wizard/">wizard docs</a>)</td></tr>
        <tr><td><code><a href="/docs/patterns/forms/wizard/#markup-reference">data-wizard-optional</a></code></td><td>Step can be skipped without validation (<a href="/docs/patterns/forms/wizard/">wizard docs</a>)</td></tr>
        <tr><td><code><a href="/docs/patterns/forms/wizard/#markup-reference">data-wizard-nav</a></code></td><td>Navigation container with prev/next/submit buttons (<a href="/docs/patterns/forms/wizard/">wizard docs</a>)</td></tr>
        <tr><td><code><a href="/docs/patterns/forms/wizard/#markup-reference">data-wizard-progress</a></code></td><td>Auto-updated <code>&lt;progress&gt;</code> bar for wizard completion (<a href="/docs/patterns/forms/wizard/">wizard docs</a>)</td></tr>
        <tr><td><code><a href="/docs/patterns/forms/wizard/#markup-reference">data-wizard-steps</a></code></td><td>Point form to a <code>nav.steps</code> element for auto-sync (<a href="/docs/patterns/forms/wizard/">wizard docs</a>)</td></tr>
      </tbody>
    </table>

    <h3>Text Visual Effects</h3>
    <p>Declarative text animations and visual effects. See the <a href="/docs/attributes/data-text-effects/">full documentation</a> for all options.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#gradient-text">data-gradient-text</a></code></td><td>CSS-only gradient coloring with theme presets (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#shimmer">data-shimmer</a></code></td><td>CSS-only shimmering highlight sweep (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#glitch">data-glitch</a></code></td><td>Chromatic aberration glitch effect (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#reveal">data-reveal</a></code></td><td>Word/line-by-line entrance animation (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#blur-reveal">data-blur-reveal</a></code></td><td>Blur-to-clear word/line reveal (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#highlight">data-highlight</a></code></td><td>Animated underline, box, or circle highlight (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#typewriter">data-typewriter</a></code></td><td>Character-by-character typing with cursor (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
        <tr><td><code><a href="/docs/attributes/data-text-effects/#scramble">data-scramble</a></code></td><td>Left-to-right character decode effect (<a href="/docs/attributes/data-text-effects/">docs</a>)</td></tr>
      </tbody>
    </table>

    <h3 id="density">Density Modes</h3>
    <p>Use <code>data-layout-density</code> on any container to adjust spacing for all children. Cascades to all descendants.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Value</th>
          <th>Effect</th>
          <th>Use Case</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>compact</code></td><td>~25% smaller spacing, tighter line-height</td><td>Dashboards, data tables, admin UIs</td></tr>
        <tr><td>(default)</td><td>Standard spacing</td><td>General use</td></tr>
        <tr><td><code>spacious</code></td><td>~50% larger spacing, relaxed line-height</td><td>Marketing pages, landing pages</td></tr>
      </tbody>
    </table>

    <p><a href="/docs/examples/demos/density-basic.html">View the density demo &rarr;</a></p>

    <h3>Grid Area Override</h3>
    <p>Use <code>data-layout-area</code> to explicitly assign elements to named areas:</p>
    <table class="props-table">
      <thead>
        <tr>
          <th>Value</th>
          <th>Use Case</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code>hero</code></td><td>Hero section on landing pages</td></tr>
        <tr><td><code>banner</code></td><td>Banner or announcement area</td></tr>
        <tr><td><code>sidebar</code></td><td>Generic sidebar area</td></tr>
        <tr><td><code>content</code></td><td>Main content area</td></tr>
        <tr><td><code>feature</code></td><td>Feature section</td></tr>
        <tr><td><code>cta</code></td><td>Call-to-action section</td></tr>
        <tr><td><code>toc</code></td><td>Table of contents</td></tr>
      </tbody>
    </table>

    <layout-text>
      <p>Named areas are used with the <code>landing</code> page template which defines grid rows for <code>hero</code>, <code>feature</code>, and <code>cta</code>. See the <a href="/docs/grid-identity/#named-areas">Grid Identity</a> page for details and the <a href="/docs/examples/demos/page-layout-landing.html">landing page demo</a>.</p>
    </layout-text>
  </section>

  <!-- Responsive Behavior -->
  <section>
    <h2>Responsive Behavior</h2>
    <p>All page layouts automatically collapse to a single column on viewports narrower than 768px, unless <code>data-layout-nowrap</code> is set.</p>

    <h3>Container Queries</h3>
    <p>Semantic sections (<code>main</code>, <code>article</code>, <code>section</code>, <code>aside</code>) have container queries enabled. Nested layouts can respond to their container width, not just viewport.</p>
  </section>

  <!-- Layout Composition -->
  <section id="layout-composition">
    <h2>Layout Composition</h2>
    <layout-text>
      <p>The <code>data-layout</code> attribute applies layout behavior to any HTML element. Instead of wrapping content in custom element tags like <code>&lt;layout-stack&gt;</code> or <code>&lt;layout-center&gt;</code>, you can use semantic HTML elements with data attributes. This reduces nesting and improves accessibility.</p>
    </layout-text>

    <h3>The data-layout attribute</h3>
    <layout-text>
      <p>Add <code>data-layout="center"</code>, <code>data-layout="stack"</code>, <code>data-layout="grid"</code>, or any other layout name to turn a plain HTML element into a layout container. All the same modifier attributes (<code>data-layout-gap</code>, <code>data-layout-max</code>, <code>data-layout-min</code>, etc.) work exactly as they do on the custom elements.</p>
    </layout-text>

    <h3>Before and after</h3>
    <layout-text>
      <p>Using data attributes, you can often eliminate wrapper elements entirely. Center with gap replaces the need for a center wrapping a stack, because <code>data-layout-gap</code> on a center element automatically enables flex-column spacing.</p>
    </layout-text>

    <h4>Before: custom element wrappers</h4>
    <code-block language="html" label="Nested custom elements">{compositionBeforeExample}</code-block>

    <h4>After: semantic HTML with data attributes</h4>
    <code-block language="html" label="Flat semantic HTML">{compositionAfterExample}</code-block>

    <layout-text>
      <p>The "after" version uses a single <code>&lt;section&gt;</code> that is both centered and vertically spaced, removing an entire layer of nesting. The grid is a plain <code>&lt;div&gt;</code> instead of a custom element.</p>
    </layout-text>

    <h3>Visual components remain as custom elements</h3>
    <layout-text>
      <p>Layout attributes replace <em>structural</em> wrappers (<code>&lt;layout-center&gt;</code>, <code>&lt;layout-stack&gt;</code>, <code>&lt;layout-grid&gt;</code>, etc.). Visual components that carry their own styles and behavior &mdash; such as <code>&lt;layout-card&gt;</code>, <code>&lt;layout-badge&gt;</code>, and <code>&lt;layout-text&gt;</code> &mdash; remain as custom elements because they encapsulate appearance, not just layout.</p>
    </layout-text>
  </section>

  <!-- Next Steps -->
  <section>
    <h2>Next Steps</h2>
    <layout-grid data-layout-min="200px" data-layout-gap="m">
      <a href="/docs/tokens/" class="section-card">
        <h3>Tokens</h3>
        <p>CSS custom properties for consistent design.</p>
      </a>
      <a href="/docs/themes/" class="section-card">
        <h3>Themes</h3>
        <p>Apply color and personality themes.</p>
      </a>
      <a href="/docs/elements/" class="section-card">
        <h3>Elements</h3>
        <p>Browse layout custom elements.</p>
      </a>
    </layout-grid>
  </section>
</DocsLayout>
