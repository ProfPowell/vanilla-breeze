---
/**
 * Experiments Gallery
 *
 * Advanced patterns exploring the boundaries of CSS and minimal JavaScript.
 */

import LabLayout from '../../../layouts/LabLayout.astro';
---

<LabLayout
  title="Experiments"
  description="Advanced patterns exploring the boundaries of CSS and minimal JavaScript."
>
  <layout-center>
    <layout-stack>
      <nav class="breadcrumb">
        <a href="/lab/">Lab</a>
        <span>/</span>
        <span>Experiments</span>
      </nav>

      <header>
        <h1>Experiments</h1>
        <p class="intro">
          Advanced patterns exploring the boundaries of CSS and minimal JavaScript.
          These demos showcase modern web platform capabilities.
        </p>
      </header>

      <div class="demo-grid">
        <a href="/lab/experiments/layout-switcher/" class="demo-card">
          <span class="demo-icon">&#128736;</span>
          <h2>Layout Switcher</h2>
          <p>Pure CSS layout switching between sidebar, holy-grail, and more layouts.</p>
          <span class="badge pure-css">Pure CSS</span>
        </a>

        <a href="/lab/experiments/theme-builder/" class="demo-card">
          <span class="demo-icon">&#127912;</span>
          <h2>Theme Builder</h2>
          <p>Interactive theme creation with live CSS variable updates.</p>
          <span class="badge minimal-js">Minimal JS</span>
        </a>

        <a href="/lab/experiments/component-lab/" class="demo-card">
          <span class="demo-icon">&#128230;</span>
          <h2>Component Lab</h2>
          <p>Interactive playground for testing component variations.</p>
          <span class="badge interactive">Interactive</span>
        </a>

        <a href="/lab/experiments/dashboard/" class="demo-card">
          <span class="demo-icon">&#128202;</span>
          <h2>Dashboard</h2>
          <p>Dashboard layout with collapsible sidebar and responsive panels.</p>
          <span class="badge minimal-js">Minimal JS</span>
        </a>
      </div>
    </layout-stack>
  </layout-center>
</LabLayout>

<style>
  nav.breadcrumb {
    display: flex;
    gap: var(--size-xs);
    font-size: var(--size-s);
    color: var(--color-text-muted);
    margin-block-end: var(--size-l);
  }

  nav.breadcrumb a {
    color: var(--color-primary);
    text-decoration: none;
  }

  header {
    margin-block-end: var(--size-xl);
  }

  h1 {
    font-size: var(--size-2xl);
    margin-block-end: var(--size-s);
  }

  .intro {
    font-size: var(--size-m);
    color: var(--color-text-muted);
    max-width: 60ch;
  }

  .demo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--size-m);
  }

  .demo-card {
    display: block;
    padding: var(--size-l);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-m);
    text-decoration: none;
    color: inherit;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }

  .demo-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  }

  .demo-icon {
    font-size: 2rem;
    display: block;
    margin-block-end: var(--size-s);
  }

  .demo-card h2 {
    margin: 0 0 var(--size-xs) 0;
    font-size: var(--size-l);
  }

  .demo-card p {
    margin: 0 0 var(--size-s) 0;
    font-size: var(--size-s);
    color: var(--color-text-muted);
  }

  .badge {
    display: inline-block;
    font-size: 0.75rem;
    padding: 0.125rem 0.5rem;
    border-radius: 9999px;
    font-weight: 500;
  }

  .badge.pure-css {
    background: #dcfce7;
    color: #166534;
  }

  .badge.minimal-js {
    background: #dbeafe;
    color: #1e40af;
  }

  .badge.interactive {
    background: #fef3c7;
    color: #92400e;
  }
</style>
