---
/**
 * Custom Fonts Demo
 *
 * External font loading with Google Fonts, variable fonts,
 * display/body pairings, and font loading API patterns.
 */

import LabLayout from '../../../layouts/LabLayout.astro';

// Code examples
const preconnectCode = `<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">`;

const fontTokensCode = `/* Theme font tokens */
--font-display: "Playfair Display", Georgia, serif;
--font-body: "Inter", system-ui, sans-serif;
--font-mono: "JetBrains Mono", ui-monospace, monospace;`;

const codeExample = `const greeting = "Hello, World!";
function fibonacci(n) {
  return n <= 1 ? n : fibonacci(n - 1) + fibonacci(n - 2);
}`;
---

<LabLayout
  title="Custom Fonts"
  description="External font loading with Google Fonts, variable fonts, and font loading patterns."
>
  <link rel="preconnect" href="https://fonts.googleapis.com" slot="head" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin slot="head" />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&family=Abril+Fatface&family=Bebas+Neue&display=swap" rel="stylesheet" slot="head" />

  <nav class="breadcrumb" aria-label="Breadcrumb">
    <a href="/lab/">Lab</a> / <a href="/lab/theme-extensions/">Theme Extensions</a> / <span aria-current="page">Custom Fonts</span>
  </nav>

  <h1>Custom Fonts</h1>
  <p class="lead">External font loading with Google Fonts, variable fonts, display/body pairings, and font loading API patterns.</p>

  <section>
    <h2>Font Pairings</h2>
    <p>Popular combinations of display and body fonts.</p>
    <browser-window url="font-pairings">
      <div class="pairing-grid">
        <div class="pairing-card">
          <h3 class="font-playfair">Playfair Display</h3>
          <p class="font-inter">Paired with Inter for a classic editorial look. The contrast between serif display and sans-serif body creates visual hierarchy.</p>
        </div>
        <div class="pairing-card">
          <h3 class="font-abril">Abril Fatface</h3>
          <p class="font-inter">Bold display type with elegant curves. Works well for headlines in magazine-style layouts.</p>
        </div>
        <div class="pairing-card">
          <h3 class="font-bebas">BEBAS NEUE</h3>
          <p class="font-inter">All-caps condensed display font. Perfect for bold, modern headlines with impact.</p>
        </div>
      </div>
    </browser-window>
  </section>

  <section>
    <h2>Monospace Fonts</h2>
    <p>Code-friendly fonts with ligatures and clear distinction.</p>
    <browser-window url="monospace-demo">
      <div class="code-samples">
        <div class="code-card">
          <h4>JetBrains Mono</h4>
          <pre class="font-jetbrains"><code>{codeExample}</code></pre>
        </div>
      </div>
    </browser-window>
  </section>

  <section>
    <h2>Font Tokens</h2>
    <p>Theme-level font configuration tokens.</p>
    <table class="props-table">
      <thead>
        <tr>
          <th>Token</th>
          <th>Value</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>--font-display</code></td>
          <td><code>"Playfair Display", Georgia, serif</code></td>
        </tr>
        <tr>
          <td><code>--font-body</code></td>
          <td><code>"Inter", system-ui, sans-serif</code></td>
        </tr>
        <tr>
          <td><code>--font-mono</code></td>
          <td><code>"JetBrains Mono", ui-monospace, monospace</code></td>
        </tr>
      </tbody>
    </table>
    <code-block language="css" label="Font token definitions">{fontTokensCode}</code-block>
  </section>

  <section>
    <h2>Loading Strategy</h2>
    <p>Best practices for font loading performance:</p>
    <ol class="loading-tips">
      <li><strong>Preconnect early:</strong> Use <code>&lt;link rel="preconnect"&gt;</code> to establish connections to font servers before they're needed.</li>
      <li><strong>Use font-display:</strong> Set <code>font-display: swap</code> to show fallback text immediately while fonts load.</li>
      <li><strong>Subset fonts:</strong> Only load the character sets you need (e.g., <code>&amp;subset=latin</code>) to reduce file size.</li>
      <li><strong>Limit weights:</strong> Load only the font weights you'll actually use rather than the full family.</li>
    </ol>
    <code-block language="html" label="Font loading setup">{preconnectCode}</code-block>
  </section>
</LabLayout>

<style>
  /* Font classes */
  .font-playfair { font-family: "Playfair Display", Georgia, serif; }
  .font-inter { font-family: "Inter", system-ui, sans-serif; }
  .font-abril { font-family: "Abril Fatface", cursive; }
  .font-bebas { font-family: "Bebas Neue", sans-serif; }
  .font-jetbrains { font-family: "JetBrains Mono", monospace; }

  .pairing-grid {
    display: grid;
    gap: var(--size-m);
    padding: var(--size-m);
  }

  .pairing-card {
    padding: var(--size-l);
    background: var(--color-surface-alt);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-m);
  }

  .pairing-card h3 {
    font-size: 2rem;
    margin: 0 0 var(--size-s) 0;
  }

  .pairing-card p {
    margin: 0;
    line-height: 1.6;
    color: var(--color-text-muted);
  }

  .code-samples {
    display: grid;
    gap: var(--size-m);
    padding: var(--size-m);
  }

  .code-card {
    padding: var(--size-m);
    background: var(--color-surface-alt);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-m);
  }

  .code-card h4 {
    margin: 0 0 var(--size-s) 0;
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  .code-card pre {
    margin: 0;
    padding: var(--size-m);
    background: #1e293b;
    border-radius: var(--radius-s);
    overflow-x: auto;
    color: #e2e8f0;
    font-size: var(--font-size-sm);
    line-height: 1.6;
  }

  .loading-tips {
    margin: var(--size-m) 0;
    padding-left: var(--size-l);
  }

  .loading-tips li {
    margin-block-end: var(--size-s);
    line-height: 1.6;
  }

  .loading-tips code {
    background: var(--color-surface-alt);
    padding: var(--size-3xs) var(--size-2xs);
    border-radius: var(--radius-s);
    font-size: var(--font-size-sm);
  }
</style>
