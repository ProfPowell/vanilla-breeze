{
  "name": "vanilla-breeze-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:doc-extras && ELEVENTY_ENV=development npx @11ty/eleventy --serve --port=4321",
    "build": "npx @11ty/eleventy",
    "build:doc-extras": "node scripts/build-doc-extras.js",
    "preview": "npx @11ty/eleventy --serve --port=4321"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "esbuild": "^0.25.0"
  }
}
