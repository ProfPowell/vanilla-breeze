---
layout: layouts/element.njk
title: "output"
description: "Displays the result of a calculation or user action. Semantically ideal for form validation messages, computed values, and dynamic results. Used extensively in the form-field custom element for validation feedback."
elementName: output
category: native
nativeGroup: Forms
currentPath: /docs/elements/native/output/
permalink: /docs/elements/native/output/
---

{% set basicOutputCode %}<label>Calculated Total</label>
<output>$124.99</output>{% endset %}

{% set styleVariantsCode %}<output>Default</output>
<output class="block">Block output</output>
<output class="inline">Inline</output>
<output class="highlight">Highlighted</output>
<output class="large">Large</output>{% endset %}

{% set stateVariantsCode %}<output class="success">Operation completed</output>
<output class="warning">Please review</output>
<output class="error">Invalid input</output>{% endset %}

{% set validationCode %}<form-field>
  <label for="email">Email</label>
  <input type="email"
         id="email"
         required
         aria-describedby="email-msg" />
  <output id="email-msg" for="email" aria-live="polite">
    Enter a valid email address
  </output>
</form-field>{% endset %}

{% set rangeSliderCode %}<label>Volume: <output id="volume">50</output>%</label>
<input type="range" min="0" max="100" value="50"
       oninput="document.getElementById('volume').value = this.value" />{% endset %}

{% set calculatedValuesCode %}<input type="number" id="qty" value="2" />
<input type="number" id="price" value="29.99" />
<output for="qty price" class="large highlight">$59.98</output>{% endset %}

{% set liveStatusCode %}<output aria-live="polite">Connected</output>
<output aria-live="polite">All changes saved</output>{% endset %}

{% set cssReferenceCode %}output {
  display: inline-block;
  font-family: var(--font-mono);
  padding: var(--size-2xs) var(--size-xs);
  background: var(--color-surface-raised);
  border-radius: var(--radius-s);
}

output.block {
  display: block;
  padding: var(--size-s) var(--size-m);
}

output.inline {
  padding: 0;
  background: transparent;
}

output.highlight {
  background: oklch(from var(--color-interactive) l c h / 0.1);
  color: var(--color-interactive);
}

output.large {
  font-size: var(--font-size-xl);
  padding: var(--size-s) var(--size-m);
}

output.success {
  background: oklch(60% 0.18 145 / 0.1);
  color: oklch(45% 0.15 145);
}

output.warning {
  background: oklch(75% 0.18 75 / 0.1);
  color: oklch(55% 0.15 75);
}

output.error {
  background: oklch(55% 0.2 25 / 0.1);
  color: oklch(50% 0.18 25);
}{% endset %}

<section>
    <h2>When to Use</h2>
    <ul>
      <li>Form validation messages (see <a href="/docs/elements/custom-elements/form-field/">form-field</a>)</li>
      <li>Calculated totals or results</li>
      <li>Dynamic values that change based on user input</li>
      <li>Displaying range slider values</li>
      <li>Any result that is derived from other form controls</li>
    </ul>
    <p><strong>Why output over span/div?</strong> The output element has built-in accessibility semantics and can be associated with inputs via the <code>for</code> attribute.</p>
  </section>

  <section>
    <h2>Basic Usage</h2>
    <p>Display a simple calculated or dynamic value.</p>

    <div class="example">
      <layout-stack data-layout-gap="m" style="max-inline-size: 25rem;">
        <form-field>
          <label>Calculated Total</label>
          <output>$124.99</output>
        </form-field>
        <form-field>
          <label>Result</label>
          <output>42</output>
        </form-field>
      </layout-stack>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Basic output usage">{{ basicOutputCode | safe }}</code-block>
  </section>

  <section>
    <h2>Attributes</h2>
    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Purpose</th>
          <th>Example</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>for</code></td>
          <td>Space-separated IDs of related inputs</td>
          <td><code>for="qty price"</code></td>
        </tr>
        <tr>
          <td><code>form</code></td>
          <td>Associates with a form by ID</td>
          <td><code>form="order-form"</code></td>
        </tr>
        <tr>
          <td><code>name</code></td>
          <td>Name for form submission</td>
          <td><code>name="total"</code></td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Style Variants</h2>

    <h3>Default</h3>
    <p>Inline monospace text with subtle background.</p>
    <div class="example">
      <output>Default output</output>
    </div>

    <h3>.block</h3>
    <p>Block-level output for larger content.</p>
    <div class="example">
      <output class="block">This is a block-level output with more content that spans the full width.</output>
    </div>

    <h3>.inline</h3>
    <p>Minimal inline output without background.</p>
    <div class="example">
      <p>Your total is <output class="inline">$99.99</output> including tax.</p>
    </div>

    <h3>.highlight</h3>
    <p>Emphasized output with brand color.</p>
    <div class="example">
      <output class="highlight">$1,234.56</output>
    </div>

    <h3>.large</h3>
    <p>Prominent display for important values.</p>
    <div class="example">
      <output class="large">$2,499.00</output>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Output style variants">{{ styleVariantsCode | safe }}</code-block>
  </section>

  <section>
    <h2>State Variants</h2>
    <p>Semantic colors for different message types.</p>

    <div class="example">
      <layout-cluster data-layout-gap="m">
        <output class="success">Success</output>
        <output class="warning">Warning</output>
        <output class="error">Error</output>
      </layout-cluster>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Output state variants">{{ stateVariantsCode | safe }}</code-block>
  </section>

  <section>
    <h2>For Validation Messages</h2>
    <p>The primary use case in Vanilla Breeze. See <a href="/docs/elements/custom-elements/form-field/">form-field</a> for the complete pattern.</p>

    <div class="example">
      <form style="max-inline-size: 25rem;">
        <form-field>
          <label for="output-email">Email</label>
          <input type="email"
                 id="output-email"
                 name="email"
                 required
                 autocomplete="email"
                 aria-describedby="output-email-msg" />
          <output id="output-email-msg" for="output-email" aria-live="polite">
            Enter a valid email address
          </output>
        </form-field>
      </form>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Output for form validation">{{ validationCode | safe }}</code-block>
  </section>

  <section>
    <h2>With Range Slider</h2>
    <p>Display the current value of a range input.</p>

    <div class="example">
      <form-field style="max-inline-size: 25rem;">
        <label for="range-slider">Volume: <output id="volume-value">50</output>%</label>
        <input type="range" id="range-slider" min="0" max="100" value="50" oninput="document.getElementById('volume-value').value = this.value" />
      </form-field>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Output with range slider">{{ rangeSliderCode | safe }}</code-block>
  </section>

  <section>
    <h2>Calculated Values</h2>
    <p>Display computed results from multiple inputs.</p>

    <div class="example">
      <form class="stacked" style="max-inline-size: 25rem;">
        <form-field>
          <label for="calc-qty">Quantity</label>
          <input type="number" id="calc-qty" value="2" min="1" />
        </form-field>
        <form-field>
          <label for="calc-price">Price per item</label>
          <input type="number" id="calc-price" value="29.99" step="0.01" min="0" />
        </form-field>
        <form-field>
          <label>Total</label>
          <output class="large highlight" for="calc-qty calc-price">$59.98</output>
        </form-field>
      </form>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Output for calculated values">{{ calculatedValuesCode | safe }}</code-block>
  </section>

  <section>
    <h2>Live Status Updates</h2>
    <p>Use <code>aria-live</code> for dynamic content that should be announced.</p>

    <div class="example">
      <layout-stack data-layout-gap="m" style="max-inline-size: 25rem;">
        <form-field>
          <label>Connection Status</label>
          <output class="success" aria-live="polite">Connected</output>
        </form-field>
        <form-field>
          <label>Save Status</label>
          <output aria-live="polite">All changes saved</output>
        </form-field>
      </layout-stack>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Live status updates">{{ liveStatusCode | safe }}</code-block>
  </section>

  <section>
    <h2>Why Output for Validation?</h2>
    <p>The <code>&lt;output&gt;</code> element is semantically ideal for form messages.</p>

    <table class="props-table">
      <thead>
        <tr>
          <th>Approach</th>
          <th>Semantics</th>
          <th>Accessibility</th>
          <th>Recommendation</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>&lt;output&gt;</code></td>
          <td>Result of action</td>
          <td>Native support with <code>for</code> attribute</td>
          <td>Recommended</td>
        </tr>
        <tr>
          <td><code>&lt;span class="error"&gt;</code></td>
          <td>None</td>
          <td>Requires manual ARIA</td>
          <td>Avoid</td>
        </tr>
        <tr>
          <td><code>&lt;div class="message"&gt;</code></td>
          <td>None</td>
          <td>Requires manual ARIA</td>
          <td>Avoid</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Accessibility Notes</h2>
    <ul>
      <li><strong>Use <code>for</code> attribute</strong>: Associates output with related input(s)</li>
      <li><strong>Add <code>aria-live</code></strong>: Use <code>polite</code> for updates that should be announced</li>
      <li><strong>Link with <code>aria-describedby</code></strong>: Connect inputs to their validation outputs</li>
      <li><strong>Don't use for static content</strong>: Output is for dynamic/calculated values</li>
      <li><strong>Include meaningful content</strong>: Always have useful text, not just icons</li>
    </ul>
  </section>

  <section>
    <h2>CSS Reference</h2>
    <code-block language="css" show-lines label="Output CSS styles">{{ cssReferenceCode | safe }}</code-block>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/custom-elements/form-field/">form-field</a> - Uses output for validation messages</li>
      <li><a href="/docs/elements/native/meter/">meter</a> - Visual gauge for scalar values</li>
      <li><a href="/docs/elements/native/progress/">progress</a> - Task completion indicator</li>
      <li><a href="/docs/elements/native/input/">input</a> - Form inputs that output relates to</li>
    </ul>
  </section>
