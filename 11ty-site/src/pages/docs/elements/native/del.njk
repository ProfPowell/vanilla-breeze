---
layout: layouts/element.njk
title: "del"
description: "The del element represents text that has been deleted from a document, typically displayed with a strikethrough and used for tracking edits or showing revisions."
elementName: del
category: native
nativeGroup: Editing
currentPath: /docs/elements/native/del/
permalink: /docs/elements/native/del/
---

{% set basicDelCode %}<del>This content has been deleted.</del>{% endset %}

{% set diffVariantCode %}<del class="diff">removed code</del>{% endset %}

{% set datetimeCode %}<del datetime="2024-01-15T10:30:00">Original content</del>{% endset %}

{% set citeCode %}<del cite="https://example.com/changelog#v2">Legacy API endpoint</del>{% endset %}

{% set screenReaderCode %}"deletion, This content has been deleted, end deletion"{% endset %}

<section>
    <h2>Description</h2>
    <p>The <code>&lt;del&gt;</code> element indicates text that has been removed from a document. It's semantically meaningful for document revision tracking, edit histories, and showing changes over time. The element can optionally include <code>cite</code> and <code>datetime</code> attributes to provide context about the deletion.</p>
  </section>

  <section>
    <h2>Semantic Meaning</h2>
    <p>The del element carries specific semantic meaning:</p>
    <ul>
      <li>Indicates content that has been removed from the document</li>
      <li>Screen readers typically announce "deletion" before the content</li>
      <li>Can include metadata about when and why the deletion occurred</li>
      <li>Paired with <a href="/docs/elements/native/ins/"><code>&lt;ins&gt;</code></a> for showing document revisions</li>
    </ul>
  </section>

  <section>
    <h2>When to Use</h2>
    <ul>
      <li><strong>Document revisions:</strong> Track changes in legal documents, contracts, or policies</li>
      <li><strong>Edit history:</strong> Show what was changed in collaborative documents</li>
      <li><strong>Diff displays:</strong> Show removed code or text in version comparisons</li>
      <li><strong>Corrections:</strong> Indicate corrected information while preserving the original</li>
      <li><strong>Track changes:</strong> Similar to "track changes" in word processors</li>
    </ul>
    <h3>When NOT to Use</h3>
    <ul>
      <li><strong>Visual strikethrough only:</strong> Use <a href="/docs/elements/native/s/"><code>&lt;s&gt;</code></a> for content that's no longer relevant but wasn't deleted</li>
      <li><strong>Price reductions:</strong> Consider <code>&lt;s&gt;</code> unless tracking actual price change history</li>
    </ul>
  </section>

  <section>
    <h2>Variants</h2>

    <h3>Default</h3>
    <p>Displays with strikethrough and a subtle red background indicating deletion.</p>
    <div class="example">
      <p><del>This content has been deleted.</del></p>
    </div>
    <code-block language="html" show-lines label="Basic del element">{{ basicDelCode | safe }}</code-block>

    <h3>.diff</h3>
    <p>Enhanced styling for diff displays with padding and rounded corners.</p>
    <div class="example">
      <p><del class="diff">removed code</del></p>
    </div>
    <code-block language="html" show-lines label="Diff styling variant">{{ diffVariantCode | safe }}</code-block>
  </section>

  <section>
    <h2>Live Examples</h2>

    <h3>Document Revision</h3>
    <div class="example">
      <p>The meeting will be held on <del>Tuesday</del> <ins>Wednesday</ins> at 2pm.</p>
    </div>

    <h3>With Timestamp</h3>
    <div class="example">
      <p><del datetime="2024-01-15T10:30:00">Original content that was removed</del></p>
      <p><small>Deleted on January 15, 2024</small></p>
    </div>
    <code-block language="html" show-lines label="Del with datetime attribute">{{ datetimeCode | safe }}</code-block>

    <h3>With Citation</h3>
    <div class="example">
      <p><del cite="https://example.com/changelog#v2">Legacy API endpoint</del></p>
    </div>
    <code-block language="html" show-lines label="Del with cite attribute">{{ citeCode | safe }}</code-block>

    <h3>Code Diff Display</h3>
    <div class="example">
      <pre><code><del class="diff">const oldVariable = "deprecated";</del>
<ins class="diff">const newVariable = "current";</ins></code></pre>
    </div>

    <h3>Contract Amendment</h3>
    <div class="example">
      <p>The service fee shall be <del datetime="2024-01-01">$50</del> <ins datetime="2024-01-01">$45</ins> per month, effective immediately.</p>
    </div>

    <h3>Block-Level Deletion</h3>
    <p>The <code>&lt;del&gt;</code> element can wrap block content when used as a block element.</p>
    <div class="example">
      <del>
        <p>This entire paragraph has been removed from the document.</p>
        <p>Multiple paragraphs can be deleted together.</p>
      </del>
    </div>
  </section>

  <section>
    <h2>Key Attributes</h2>
    <table class="props-table">
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>cite</code></td>
          <td>URL to a document explaining the change</td>
        </tr>
        <tr>
          <td><code>datetime</code></td>
          <td>Date and time of the deletion (ISO 8601 format)</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>CSS Properties</h2>
    <table class="props-table">
      <thead>
        <tr>
          <th>Property</th>
          <th>Value</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>text-decoration</code></td>
          <td><code>line-through</code></td>
        </tr>
        <tr>
          <td><code>background</code></td>
          <td><code>oklch(55% 0.2 25 / 0.1)</code> (subtle red)</td>
        </tr>
        <tr>
          <td><code>text-decoration-color</code></td>
          <td><code>oklch(55% 0.2 25)</code> (red)</td>
        </tr>
      </tbody>
    </table>

    <h3>.diff Variant</h3>
    <table class="props-table">
      <thead>
        <tr>
          <th>Property</th>
          <th>Value</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>display</code></td>
          <td><code>inline-block</code></td>
        </tr>
        <tr>
          <td><code>padding-inline</code></td>
          <td><code>var(--size-2xs)</code></td>
        </tr>
        <tr>
          <td><code>border-radius</code></td>
          <td><code>var(--radius-s)</code></td>
        </tr>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Accessibility</h2>
    <ul>
      <li><strong>Screen reader announcement:</strong> Most screen readers announce "deletion" before reading deleted content</li>
      <li><strong>Visual indication:</strong> Strikethrough and background color provide visual cues</li>
      <li><strong>Don't hide important info:</strong> Ensure deleted content that's still visible is accessible</li>
      <li><strong>Datetime attribute:</strong> Helps users understand when changes occurred</li>
    </ul>

    <h3>Screen Reader Behavior</h3>
    <p>With proper screen reader settings, users will hear something like:</p>
    <code-block language="html" show-lines label="Screen reader announcement">{{ screenReaderCode | safe }}</code-block>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/ins/"><code>&lt;ins&gt;</code></a> - Companion element for inserted/added text (use together for edits)</li>
      <li><a href="/docs/elements/native/s/"><code>&lt;s&gt;</code></a> - For strikethrough text that's no longer accurate but wasn't formally deleted</li>
    </ul>
  </section>
