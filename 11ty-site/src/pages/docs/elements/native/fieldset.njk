---
layout: layouts/element.njk
title: "fieldset"
description: "Groups related form controls together with an optional caption. Essential for organizing complex forms and providing accessible groupings for screen readers."
elementName: fieldset
category: native
nativeGroup: Forms
currentPath: /docs/elements/native/fieldset/
permalink: /docs/elements/native/fieldset/
---

{% set basicFieldsetCode %}<fieldset>
  <legend>Contact Information</legend>
  <layout-stack data-layout-gap="m">
    <div class="group">
      <label for="name">Full Name</label>
      <input type="text" id="name" />
    </div>
    <div class="group">
      <label for="email">Email Address</label>
      <input type="email" id="email" />
    </div>
  </layout-stack>
</fieldset>{% endset %}

{% set styleVariantsCode %}<!-- Default bordered fieldset -->
<fieldset>
  <legend>Group Title</legend>
  <!-- form fields -->
</fieldset>

<!-- Minimal/borderless fieldset -->
<fieldset class="minimal">
  <legend>Group Title</legend>
  <!-- form fields -->
</fieldset>{% endset %}

{% set radioGroupCode %}<fieldset>
  <legend>Preferred Contact Method</legend>
  <layout-stack data-layout-gap="s">
    <label>
      <input type="radio" name="contact-method" value="email" checked /> Email
    </label>
    <label>
      <input type="radio" name="contact-method" value="phone" /> Phone
    </label>
    <label>
      <input type="radio" name="contact-method" value="mail" /> Mail
    </label>
  </layout-stack>
</fieldset>{% endset %}

{% set disabledFieldsetCode %}<fieldset disabled>
  <legend>Disabled Section</legend>
  <!-- All controls inside are disabled -->
</fieldset>{% endset %}

{% set cssReferenceCode %}fieldset {
  border: var(--border-width-thin) solid var(--color-border);
  border-radius: var(--radius-m);
  padding: var(--size-m);
  margin: 0;
}

/* Minimal/borderless fieldset */
fieldset.minimal {
  border: none;
  padding: 0;
}

fieldset.minimal > legend {
  padding: 0;
  margin-block-end: var(--size-s);
  font-size: var(--font-size-md);
}

legend {
  padding-inline: var(--size-s);
  font-weight: 600;
  font-size: var(--font-size-sm);
}{% endset %}

<section>
    <h2>When to Use</h2>
    <ul>
      <li>Grouping related form fields (e.g., billing address, shipping address)</li>
      <li>Radio button or checkbox groups that belong together</li>
      <li>Sections of a multi-part form</li>
      <li>Any time you need to provide a group label for accessibility</li>
    </ul>
    <p><strong>Always include a <a href="/docs/elements/native/legend/">legend</a></strong> as the first child to provide a caption for the group.</p>
  </section>

  <section>
    <h2>Basic Usage</h2>
    <p>A fieldset with a legend creates a visually and semantically grouped form section.</p>

    <div class="example">
      <fieldset>
        <legend>Contact Information</legend>
        <layout-stack data-layout-gap="m">
          <form-field>
            <label for="fs-name">Full Name</label>
            <input type="text" id="fs-name" />
          </form-field>
          <form-field>
            <label for="fs-email">Email Address</label>
            <input type="email" id="fs-email" />
          </form-field>
          <form-field>
            <label for="fs-phone">Phone Number</label>
            <input type="tel" id="fs-phone" />
          </form-field>
        </layout-stack>
      </fieldset>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Basic fieldset with legend">{{ basicFieldsetCode | escape }}</code-block>
  </section>

  <section>
    <h2>Style Variants</h2>

    <h3>Default (Bordered)</h3>
    <p>Standard fieldset with a visible border and legend that breaks the border line.</p>
    <div class="example">
      <fieldset>
        <legend>Shipping Address</legend>
        <layout-stack data-layout-gap="m">
          <form-field>
            <label for="fs-street">Street Address</label>
            <input type="text" id="fs-street" />
          </form-field>
          <form-field>
            <label for="fs-city">City</label>
            <input type="text" id="fs-city" />
          </form-field>
        </layout-stack>
      </fieldset>
    </div>

    <h3>.minimal</h3>
    <p>Borderless fieldset for subtle grouping. The legend appears as a heading.</p>
    <div class="example">
      <fieldset class="minimal">
        <legend>Notification Preferences</legend>
        <layout-stack data-layout-gap="s">
          <label>
            <input type="checkbox" name="notify" value="email" /> Email notifications
          </label>
          <label>
            <input type="checkbox" name="notify" value="sms" /> SMS notifications
          </label>
          <label>
            <input type="checkbox" name="notify" value="push" /> Push notifications
          </label>
        </layout-stack>
      </fieldset>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Fieldset style variants">{{ styleVariantsCode | escape }}</code-block>
  </section>

  <section>
    <h2>Radio Button Groups</h2>
    <p>Fieldset is essential for grouping radio buttons. Screen readers will announce the legend before each radio option.</p>

    <div class="example">
      <fieldset>
        <legend>Preferred Contact Method</legend>
        <layout-stack data-layout-gap="s">
          <label>
            <input type="radio" name="contact-method" value="email" checked /> Email
          </label>
          <label>
            <input type="radio" name="contact-method" value="phone" /> Phone
          </label>
          <label>
            <input type="radio" name="contact-method" value="mail" /> Mail
          </label>
        </layout-stack>
      </fieldset>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Radio button group in fieldset">{{ radioGroupCode | escape }}</code-block>
  </section>

  <section>
    <h2>Checkbox Groups</h2>
    <p>Group related checkboxes together to indicate they form a set.</p>

    <div class="example">
      <fieldset class="minimal">
        <legend>Select your interests</legend>
        <layout-stack data-layout-gap="s">
          <label>
            <input type="checkbox" name="interests" value="tech" /> Technology
          </label>
          <label>
            <input type="checkbox" name="interests" value="design" /> Design
          </label>
          <label>
            <input type="checkbox" name="interests" value="business" /> Business
          </label>
          <label>
            <input type="checkbox" name="interests" value="science" /> Science
          </label>
        </layout-stack>
      </fieldset>
    </div>
  </section>

  <section>
    <h2>Nested Fieldsets</h2>
    <p>Fieldsets can be nested for complex forms with sub-groupings.</p>

    <div class="example">
      <fieldset>
        <legend>Account Settings</legend>
        <layout-stack data-layout-gap="l">
          <fieldset class="minimal">
            <legend>Profile</legend>
            <layout-stack data-layout-gap="m">
              <form-field>
                <label for="fs-username">Username</label>
                <input type="text" id="fs-username" />
              </form-field>
              <form-field>
                <label for="fs-bio">Bio</label>
                <textarea id="fs-bio" rows="3"></textarea>
              </form-field>
            </layout-stack>
          </fieldset>

          <fieldset class="minimal">
            <legend>Privacy</legend>
            <layout-stack data-layout-gap="s">
              <label>
                <input type="checkbox" name="privacy" value="public" /> Make profile public
              </label>
              <label>
                <input type="checkbox" name="privacy" value="searchable" /> Allow search engines to index
              </label>
            </layout-stack>
          </fieldset>
        </layout-stack>
      </fieldset>
    </div>
  </section>

  <section>
    <h2>Disabled Fieldset</h2>
    <p>The <code>disabled</code> attribute on a fieldset disables all form controls within it.</p>

    <div class="example">
      <fieldset disabled>
        <legend>Disabled Section</legend>
        <layout-stack data-layout-gap="m">
          <form-field>
            <label for="fs-disabled-name">Name</label>
            <input type="text" id="fs-disabled-name" value="Cannot edit" />
          </form-field>
          <form-field>
            <label for="fs-disabled-email">Email</label>
            <input type="email" id="fs-disabled-email" value="disabled@example.com" />
          </form-field>
          <button type="button">Cannot Click</button>
        </layout-stack>
      </fieldset>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Disabled fieldset">{{ disabledFieldsetCode | escape }}</code-block>
  </section>

  <section>
    <h2>With form-field</h2>
    <p>Combine fieldsets with the <a href="/docs/elements/custom-elements/form-field/">form-field custom element</a> for enhanced form controls.</p>

    <div class="example">
      <form style="max-inline-size: 30rem;">
        <fieldset>
          <legend>Account Details</legend>
          <layout-stack data-layout-gap="m">
            <form-field>
              <label for="ff-email">Email</label>
              <input type="email"
                     id="ff-email"
                     name="email"
                     required
                     autocomplete="email"
                     aria-describedby="ff-email-msg" />
              <output id="ff-email-msg" for="ff-email" aria-live="polite">
                Enter a valid email address
              </output>
            </form-field>

            <form-field>
              <label for="ff-password">Password</label>
              <input type="password"
                     id="ff-password"
                     name="password"
                     required
                     minlength="8"
                     autocomplete="new-password"
                     aria-describedby="ff-password-msg" />
              <output id="ff-password-msg" for="ff-password" aria-live="polite">
                At least 8 characters
              </output>
            </form-field>
          </layout-stack>
        </fieldset>
      </form>
    </div>
  </section>

  <section>
    <h2>Accessibility Notes</h2>
    <ul>
      <li><strong>Always include a legend</strong>: The <a href="/docs/elements/native/legend/">legend</a> provides the accessible name for the group</li>
      <li><strong>Legend must be first child</strong>: For proper rendering, legend must be the first element inside fieldset</li>
      <li><strong>Screen reader behavior</strong>: The legend is announced before each control in the group</li>
      <li><strong>Required for radio groups</strong>: Radio buttons should always be in a fieldset for accessibility</li>
      <li><strong>Avoid nesting too deep</strong>: Limit nesting to maintain comprehension</li>
    </ul>
  </section>

  <section>
    <h2>CSS Reference</h2>
    <code-block language="css" show-lines label="CSS for fieldset styling">{{ cssReferenceCode | escape }}</code-block>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/legend/">legend</a> - Caption for the fieldset (required first child)</li>
      <li><a href="/docs/elements/native/form/">form</a> - Container for fieldsets</li>
      <li><a href="/docs/elements/native/input/">input</a> - Form controls within fieldsets</li>
      <li><a href="/docs/elements/custom-elements/form-field/">form-field</a> - Enhanced form field wrapper</li>
    </ul>
  </section>
