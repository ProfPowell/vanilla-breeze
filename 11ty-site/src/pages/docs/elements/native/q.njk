---
layout: layouts/element.njk
title: "q"
description: "Represents an inline quotation."
elementName: q
category: native
nativeGroup: Text Semantics
currentPath: /docs/elements/native/q/
permalink: /docs/elements/native/q/
---

{% set basicQCode %}<p>She said <q>Hello, world!</q> and smiled.</p>{% endset %}

{% set citedQCode %}<p>
  According to the documentation,
  <q cite="https://example.com/docs">this feature is experimental</q>.
</p>{% endset %}

{% set nestedQCode %}<p>
  He asked, <q>Did she really say <q>I'll be there</q>?</q>
</p>{% endset %}

{% set languageQCode %}<p>
  The French phrase <q lang="fr">C'est la vie</q> means "That's life."
</p>{% endset %}

<section>
    <h2>Description</h2>
    <p>The <code>&lt;q&gt;</code> element indicates that the enclosed text is a short inline quotation. Most modern browsers implement this by surrounding the text in quotation marks. This element is intended for short quotations that don't require paragraph breaks; for longer quotations use the <a href="/docs/elements/native/blockquote/"><code>&lt;blockquote&gt;</code></a> element.</p>
  </section>

  <section>
    <h2>Basic Example</h2>
    <div class="example">
      <p>She said <q>Hello, world!</q> and smiled.</p>
    </div>
    <code-block language="html" label="Basic q example">{{ basicQCode | safe }}</code-block>
  </section>

  <section>
    <h2>With Citation</h2>
    <p>The <code>cite</code> attribute provides a URL for the source of the quotation:</p>
    <div class="example">
      <p>
        According to the documentation,
        <q cite="https://example.com/docs">this feature is experimental</q>.
      </p>
    </div>
    <code-block language="html" label="Q with cite attribute">{{ citedQCode | safe }}</code-block>
  </section>

  <section>
    <h2>Nested Quotations</h2>
    <p>Browsers will alternate between single and double quotation marks for nested quotes:</p>
    <div class="example">
      <p>
        He asked, <q>Did she really say <q>I'll be there</q>?</q>
      </p>
    </div>
    <code-block language="html" label="Nested quotations">{{ nestedQCode | safe }}</code-block>
  </section>

  <section>
    <h2>With Language Attribute</h2>
    <p>Use the <code>lang</code> attribute to specify the language of the quotation. Browsers may use language-appropriate quotation marks:</p>
    <div class="example">
      <p>
        The French phrase <q lang="fr">C'est la vie</q> means "That's life."
      </p>
    </div>
    <code-block language="html" label="Q with language">{{ languageQCode | safe }}</code-block>
  </section>

  <section>
    <h2>Attributes</h2>
    <table>
      <thead>
        <tr>
          <th>Attribute</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>cite</code></td>
          <td>A URL pointing to the source of the quotation</td>
        </tr>
      </tbody>
    </table>
    <p>This element also supports <a href="/docs/elements/native/states/">global attributes</a>.</p>
  </section>

  <section>
    <h2>Accessibility</h2>
    <p>Screen readers typically announce the quotation marks that browsers add around <code>&lt;q&gt;</code> content. The semantic markup helps convey that the text is a quotation rather than regular content.</p>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/blockquote/"><code>&lt;blockquote&gt;</code></a> - For longer, block-level quotations</li>
      <li><a href="/docs/elements/native/cite/"><code>&lt;cite&gt;</code></a> - For citing the title of a work</li>
    </ul>
  </section>
