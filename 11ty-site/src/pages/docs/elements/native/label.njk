---
layout: layouts/element.njk
title: "label"
description: "Associates a text caption with a form control. Labels are essential for accessibility, enabling screen readers to announce form fields and allowing users to click the label to focus the associated input."
elementName: label
category: native
nativeGroup: Forms
currentPath: /docs/elements/native/label/
permalink: /docs/elements/native/label/
---

{% set explicitLabelCode %}<label for="name">Full Name</label>
<input type="text" id="name" />{% endset %}

{% set implicitLabelCode %}<!-- Text input wrapped -->
<label>
  Email Address
  <input type="email" />
</label>

<!-- Checkbox wrapped -->
<label>
  <input type="checkbox" /> I agree to the terms
</label>{% endset %}

{% set checkboxRadioCode %}<!-- Checkbox -->
<label>
  <input type="checkbox" name="feature" /> Enable feature
</label>

<!-- Radio buttons in fieldset -->
<fieldset>
  <legend>Select size</legend>
  <label>
    <input type="radio" name="size" value="small" /> Small
  </label>
  <label>
    <input type="radio" name="size" value="large" /> Large
  </label>
</fieldset>{% endset %}

{% set requiredFieldCode %}<p class="help">Fields marked with * are required</p>
<label for="name">Name *</label>
<input type="text" id="name" required />{% endset %}

{% set visuallyHiddenCode %}<label for="search" class="visually-hidden">Search</label>
<input type="search" id="search" placeholder="Search..." />{% endset %}

{% set cssReferenceCode %}label {
  display: block;
  font-weight: 500;
  margin-block-end: var(--size-xs);
}

/* Within a .group container */
.group > label {
  font-weight: 500;
  font-size: var(--font-size-sm);
}

/* Visually hidden but accessible */
.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}{% endset %}

<section>
    <h2>When to Use</h2>
    <ul>
      <li>Every <a href="/docs/elements/native/input/">input</a>, <a href="/docs/elements/native/select/">select</a>, and <a href="/docs/elements/native/textarea/">textarea</a> needs a label</li>
      <li>Checkboxes and radio buttons should have labels wrapping them</li>
      <li>Any form control that requires user identification</li>
    </ul>
    <p><strong>Always provide labels</strong>. Forms without labels are inaccessible to screen reader users.</p>
  </section>

  <section>
    <h2>Association Methods</h2>

    <h3>Using the <code>for</code> Attribute</h3>
    <p>The recommended method. The label's <code>for</code> attribute matches the input's <code>id</code>.</p>
    <div class="example">
      <form-field style="max-inline-size: 25rem;">
        <label for="explicit-name">Full Name</label>
        <input type="text" id="explicit-name" placeholder="Click the label to focus" />
      </form-field>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Explicit label association">{{ explicitLabelCode | escape }}</code-block>

    <h3>Implicit Association (Wrapping)</h3>
    <p>The label wraps the input. Useful for checkboxes and radios where text follows the control.</p>
    <div class="example">
      <layout-stack data-layout-gap="m" style="max-inline-size: 25rem;">
        <label>
          Email Address
          <input type="email" placeholder="Label wraps input" />
        </label>
        <label>
          <input type="checkbox" /> I agree to the terms
        </label>
      </layout-stack>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Implicit label association">{{ implicitLabelCode | escape }}</code-block>
  </section>

  <section>
    <h2>Label Placement</h2>

    <h3>Above Input (Default)</h3>
    <p>Standard placement for text inputs. Easiest to scan.</p>
    <div class="example">
      <form-field style="max-inline-size: 25rem;">
        <label for="above-input">Username</label>
        <input type="text" id="above-input" />
      </form-field>
    </div>

    <h3>Beside Input (Horizontal)</h3>
    <p>For checkboxes, radios, and compact forms.</p>
    <div class="example">
      <div class="group horizontal" style="max-inline-size: 25rem;">
        <input type="checkbox" id="beside-input" />
        <label for="beside-input">Remember my preferences</label>
      </div>
    </div>

    <h3>Left of Input (Grid Layout)</h3>
    <p>In <code>form.grid</code> layout, labels appear to the left.</p>
    <div class="example">
      <form class="grid" style="max-inline-size: 30rem;">
        <label for="left-first">First Name</label>
        <input type="text" id="left-first" />

        <label for="left-last">Last Name</label>
        <input type="text" id="left-last" />
      </form>
    </div>
  </section>

  <section>
    <h2>With Checkboxes and Radios</h2>
    <p>For checkboxes and radio buttons, wrap the input inside the label.</p>

    <h3>Checkboxes</h3>
    <div class="example">
      <layout-stack data-layout-gap="s">
        <label>
          <input type="checkbox" name="features" value="notifications" /> Enable notifications
        </label>
        <label>
          <input type="checkbox" name="features" value="newsletter" /> Subscribe to newsletter
        </label>
        <label>
          <input type="checkbox" name="features" value="updates" checked /> Receive product updates
        </label>
      </layout-stack>
    </div>

    <h3>Radio Buttons</h3>
    <div class="example">
      <fieldset class="minimal">
        <legend>Select size</legend>
        <layout-stack data-layout-gap="s">
          <label>
            <input type="radio" name="size" value="small" /> Small
          </label>
          <label>
            <input type="radio" name="size" value="medium" checked /> Medium
          </label>
          <label>
            <input type="radio" name="size" value="large" /> Large
          </label>
        </layout-stack>
      </fieldset>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Checkbox and radio labels">{{ checkboxRadioCode | escape }}</code-block>
  </section>

  <section>
    <h2>Required Field Indicator</h2>
    <p>Indicate required fields visually. The asterisk is commonly used but should be explained.</p>

    <div class="example">
      <form class="stacked" style="max-inline-size: 25rem;">
        <p class="help">Fields marked with * are required</p>
        <form-field>
          <label for="required-name">Name *</label>
          <input type="text" id="required-name" required />
        </form-field>
        <form-field>
          <label for="required-email">Email *</label>
          <input type="email" id="required-email" required />
        </form-field>
        <form-field>
          <label for="optional-phone">Phone (optional)</label>
          <input type="tel" id="optional-phone" />
        </form-field>
      </form>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Required field indicator">{{ requiredFieldCode | escape }}</code-block>
  </section>

  <section>
    <h2>Visually Hidden Labels</h2>
    <p>When a label is not visually needed (e.g., search with a placeholder), hide it visually but keep it accessible.</p>

    <div class="example">
      <form class="inline">
        <form-field>
          <label for="hidden-search" class="visually-hidden">Search</label>
          <input type="search" id="hidden-search" placeholder="Search..." />
        </form-field>
        <button type="submit">Search</button>
      </form>
    </div>

    <h3>Code</h3>
    <code-block language="html" show-lines label="Visually hidden label">{{ visuallyHiddenCode | escape }}</code-block>

    <p class="help">The <code>.visually-hidden</code> class hides the label visually while keeping it accessible to screen readers.</p>
  </section>

  <section>
    <h2>With form-field</h2>
    <p>Labels work seamlessly with the <a href="/docs/elements/custom-elements/form-field/">form-field custom element</a>.</p>

    <div class="example">
      <form style="max-inline-size: 25rem;">
        <form-field>
          <label for="ff-label-demo">Email Address</label>
          <input type="email"
                 id="ff-label-demo"
                 name="email"
                 required
                 autocomplete="email"
                 aria-describedby="ff-label-demo-msg" />
          <output id="ff-label-demo-msg" for="ff-label-demo" aria-live="polite">
            Enter a valid email
          </output>
        </form-field>
      </form>
    </div>
  </section>

  <section>
    <h2>Accessibility Notes</h2>
    <ul>
      <li><strong>Every input needs a label</strong>: No exceptions for accessibility</li>
      <li><strong>Use <code>for</code> attribute</strong>: Explicit association is clearer and more robust</li>
      <li><strong>Labels are clickable</strong>: Clicking a label focuses its associated input</li>
      <li><strong>Don't use placeholder as label</strong>: Placeholders disappear when typing</li>
      <li><strong>Multiple inputs per label</strong>: Not valid; use one label per input</li>
      <li><strong>aria-labelledby alternative</strong>: Can reference multiple elements for complex labels</li>
    </ul>
  </section>

  <section>
    <h2>CSS Reference</h2>
    <code-block language="css" show-lines label="Label CSS styles">{{ cssReferenceCode | escape }}</code-block>
  </section>

  <section>
    <h2>Related Elements</h2>
    <ul>
      <li><a href="/docs/elements/native/input/">input</a> - Form controls that labels associate with</li>
      <li><a href="/docs/elements/native/select/">select</a> - Dropdown menus that need labels</li>
      <li><a href="/docs/elements/native/textarea/">textarea</a> - Multi-line inputs that need labels</li>
      <li><a href="/docs/elements/native/fieldset/">fieldset</a> - Groups of labeled controls</li>
      <li><a href="/docs/elements/native/legend/">legend</a> - Label for a fieldset group</li>
      <li><a href="/docs/elements/custom-elements/form-field/">form-field</a> - Enhanced form field wrapper</li>
    </ul>
  </section>
