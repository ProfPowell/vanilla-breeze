---
layout: layouts/docs.njk
title: "Loading States Snippet"
description: "Spinner, skeleton, shimmer, and overlay patterns for indicating loading states."
currentSection: examples
currentPath: /docs/snippets/css/loading-states/
permalink: /docs/snippets/css/loading-states/
---

{% set spinnerCode %}<!-- Inline spinner -->
<loading-spinner data-size="s" aria-label="Loading"></loading-spinner>
Loading...

<!-- Button with spinner -->
<button disabled>
  <loading-spinner data-size="s" aria-label="Loading"></loading-spinner>
  Saving...
</button>{% endset %}

{% set skeletonCode %}<!-- Skeleton loading state -->
<div data-loading="skeleton">
  <h2>Placeholder heading</h2>
  <p>Placeholder paragraph text that shows as a gray bar.</p>
  <p>Another paragraph of placeholder text.</p>
</div>{% endset %}

{% set shimmerCode %}<!-- Shimmer overlay on existing content -->
<div data-loading>
  <h2>Real content here</h2>
  <p>Content is dimmed with a shimmer animation overlay.</p>
</div>

<!-- Minimal pulse variant -->
<div data-loading="minimal">
  <p>Subtle pulse effect instead of sweeping shimmer.</p>
</div>

<!-- Hidden content variant -->
<div data-loading="hide">
  <p>Content is completely hidden (not just dimmed).</p>
</div>{% endset %}

{% set overlayCode %}<!-- Spinner overlay on a section -->
<div style="position: relative; min-height: 200px;">
  <p>Content behind the overlay.</p>
  <loading-spinner data-overlay data-size="l" aria-label="Loading section"></loading-spinner>
</div>{% endset %}

{% set progressCode %}<!-- Determinate progress ring -->
<progress-ring style="--progress: 67" data-size="l" role="progressbar" aria-valuenow="67" aria-valuemin="0" aria-valuemax="100">
  <span>67%</span>
</progress-ring>

<!-- Indeterminate progress ring -->
<progress-ring data-indeterminate data-size="l" role="progressbar" aria-label="Loading"></progress-ring>

<!-- Native progress bar -->
<progress value="67" max="100">67%</progress>{% endset %}

<nav class="breadcrumb" aria-label="Breadcrumb">
    <ol>
      <li><a href="/docs/snippets/">Snippets</a></li>
      <li><span aria-current="page">Loading States</span></li>
    </ol>
  </nav>

  <h1>Loading States</h1>
  <p class="lead">Spinner, skeleton, shimmer, and overlay patterns for indicating loading states.</p>

  <section>
    <h2>Overview</h2>
    <layout-text>
      <p>Vanilla Breeze provides multiple loading state patterns for different contexts:</p>
      <ul>
        <li><strong>Spinner</strong> &mdash; Inline animated indicator for buttons, text, or overlays</li>
        <li><strong>Skeleton</strong> &mdash; Content-shaped placeholders that show layout structure</li>
        <li><strong>Shimmer</strong> &mdash; Animated overlay on existing content</li>
        <li><strong>Progress ring</strong> &mdash; Circular determinate/indeterminate progress</li>
      </ul>
    </layout-text>
  </section>

  <section>
    <h2>Inline Spinner</h2>
    <layout-text>
      <p>Use <code>&lt;loading-spinner&gt;</code> inline with text or inside buttons to indicate an ongoing operation.</p>
    </layout-text>

    <div class="example">
      <layout-stack data-layout-gap="m">
        <p>
          <loading-spinner data-size="s" aria-label="Loading"></loading-spinner>
          Loading...
        </p>
        <div>
          <button disabled>
            <loading-spinner data-size="s" aria-label="Loading"></loading-spinner>
            Saving...
          </button>
        </div>
      </layout-stack>
    </div>

    <code-block language="html" label="Inline spinner">{{ spinnerCode | safe }}</code-block>
  </section>

  <section>
    <h2>Skeleton Loading</h2>
    <layout-text>
      <p>Add <code>data-loading="skeleton"</code> to show children as gray placeholder shapes that preserve the layout structure.</p>
    </layout-text>

    <div class="example">
      <div data-loading="skeleton">
        <h2>Placeholder heading</h2>
        <p>Placeholder paragraph text that shows as a gray bar.</p>
        <p>Another paragraph of placeholder text.</p>
      </div>
    </div>

    <code-block language="html" label="Skeleton state">{{ skeletonCode | safe }}</code-block>
  </section>

  <section>
    <h2>Shimmer Overlay</h2>
    <layout-text>
      <p>The <code>data-loading</code> attribute (without a value, or with <code>"minimal"</code> or <code>"hide"</code>) adds an animated overlay on top of existing content.</p>
    </layout-text>

    <code-block language="html" label="Shimmer variants">{{ shimmerCode | safe }}</code-block>
  </section>

  <section>
    <h2>Spinner Overlay</h2>
    <layout-text>
      <p>Add <code>data-overlay</code> to a spinner to center it over a parent container with a translucent backdrop.</p>
    </layout-text>

    <code-block language="html" label="Overlay spinner">{{ overlayCode | safe }}</code-block>
  </section>

  <section>
    <h2>Progress Indicators</h2>
    <layout-text>
      <p>For determinate progress, use <code>&lt;progress-ring&gt;</code> or the native <code>&lt;progress&gt;</code> element.</p>
    </layout-text>

    <div class="example" style="display: flex; align-items: center; gap: var(--size-l); flex-wrap: wrap;">
      <progress-ring style="--progress: 67" data-size="l" role="progressbar" aria-valuenow="67" aria-valuemin="0" aria-valuemax="100">
        <span>67%</span>
      </progress-ring>
      <progress-ring data-indeterminate data-size="l" role="progressbar" aria-label="Loading"></progress-ring>
      <progress value="67" max="100">67%</progress>
    </div>

    <code-block language="html" label="Progress indicators">{{ progressCode | safe }}</code-block>
  </section>

  <section>
    <h2>Usage Guidelines</h2>
    <layout-text>
      <ul>
        <li><strong>Spinner:</strong> Use for short, indeterminate operations (fetching data, saving)</li>
        <li><strong>Skeleton:</strong> Use for initial page loads where content structure is known</li>
        <li><strong>Shimmer:</strong> Use for refreshing existing content (data reload)</li>
        <li><strong>Progress ring:</strong> Use when you can track completion percentage</li>
        <li>All patterns respect <code>prefers-reduced-motion: reduce</code></li>
        <li>Always include appropriate ARIA attributes for screen readers</li>
      </ul>
    </layout-text>
  </section>

  <section>
    <h2>Related</h2>
    <layout-grid data-layout-min="200px" data-layout-gap="m">
      <a href="/docs/elements/custom-elements/loading-spinner/" class="section-card">
        <h3>loading-spinner</h3>
        <p>CSS-only spinner element</p>
      </a>
      <a href="/docs/elements/custom-elements/progress-ring/" class="section-card">
        <h3>progress-ring</h3>
        <p>Circular progress indicator</p>
      </a>
      <a href="/docs/elements/native/progress/" class="section-card">
        <h3>progress</h3>
        <p>Native progress bar</p>
      </a>
      <a href="/docs/patterns/feedback/skeleton/" class="section-card">
        <h3>Skeleton Pattern</h3>
        <p>Full skeleton loading pattern</p>
      </a>
    </layout-grid>
  </section>
