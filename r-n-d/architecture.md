┌─────────────────────────────────────────────────┐
│  @layer reset        │ Baseline normalization   │
│  @layer tokens       │ Design token primitives  │
│  @layer semantic     │ Semantic token mappings  │
│  @layer identity     │ Grid identity (structure)│
│  @layer composition  │ Layout compositions      │
│  @layer theme        │ Visual theme (swappable) │
│  @layer components   │ Component styles         │
│  @layer utilities    │ Overrides                │
└─────────────────────────────────────────────────┘